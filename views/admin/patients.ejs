<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patients Management - Bizil Health Care Admin</title>
    <link rel="icon" type="image/png" href="/images/favicon.ico">
    <link rel="shortcut icon" type="image/png" href="/images/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/modern-dashboard.css">
    <style>
        body {
            background: #F8FAFC;
            font-family: 'Inter', sans-serif;
        }

        .filter-section {
            background: white;
            border-radius: 1rem;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1);
            border: 1px solid #E2E8F0;
        }

        .stats-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .stat-box {
            background: white;
            border-radius: 1rem;
            padding: 1.25rem;
            box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1);
            border: 1px solid #E2E8F0;
            text-align: center;
        }

        .stat-box h3 {
            font-size: 2rem;
            font-weight: 700;
            color: #1E293B;
            margin-bottom: 0.25rem;
        }

        .stat-box p {
            font-size: 0.875rem;
            color: #64748B;
            margin: 0;
        }

        .patients-table-container {
            background: white;
            border-radius: 1rem;
            padding: 1.5rem;
            box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1);
            border: 1px solid #E2E8F0;
        }

        .table-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #E2E8F0;
        }

        .table-header h2 {
            font-size: 1.25rem;
            font-weight: 600;
            color: #1E293B;
            margin: 0;
        }

        .patients-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
        }

        .patients-table thead th {
            background: #F8FAFC;
            color: #64748B;
            font-weight: 600;
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            padding: 1rem;
            text-align: left;
            border-bottom: 2px solid #E2E8F0;
        }

        .patients-table tbody tr {
            transition: all 0.2s;
        }

        .patients-table tbody tr:hover {
            background: #F8FAFC;
        }

        .patients-table tbody td {
            padding: 1rem;
            border-bottom: 1px solid #E2E8F0;
            color: #1E293B;
        }

        .patient-avatar {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: linear-gradient(135deg, #8B5CF6, #6366F1);
            display: inline-flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
            margin-right: 0.75rem;
        }

        .patient-info {
            display: inline-flex;
            flex-direction: column;
        }

        .patient-name {
            font-weight: 600;
            color: #1E293B;
        }

        .patient-email {
            font-size: 0.875rem;
            color: #64748B;
        }

        .badge {
            padding: 0.375rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 600;
            display: inline-block;
        }

        .badge-male {
            background: rgba(59, 130, 246, 0.1);
            color: #3B82F6;
        }

        .badge-female {
            background: rgba(236, 72, 153, 0.1);
            color: #EC4899;
        }

        .badge-other {
            background: rgba(139, 92, 246, 0.1);
            color: #8B5CF6;
        }

        .action-buttons {
            display: flex;
            gap: 0.5rem;
        }

        .btn-action {
            padding: 0.5rem 0.75rem;
            border-radius: 0.5rem;
            border: none;
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s;
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
        }

        .btn-view {
            background: rgba(59, 130, 246, 0.1);
            color: #3B82F6;
        }

        .btn-view:hover {
            background: rgba(59, 130, 246, 0.2);
        }

        .btn-delete {
            background: rgba(239, 68, 68, 0.1);
            color: #EF4444;
        }

        .btn-delete:hover {
            background: rgba(239, 68, 68, 0.2);
        }

        .search-filter-group {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            gap: 1rem;
            align-items: end;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .form-group label {
            font-size: 0.875rem;
            font-weight: 600;
            color: #1E293B;
        }

        .form-control, .form-select {
            padding: 0.625rem 1rem;
            border: 1px solid #E2E8F0;
            border-radius: 0.5rem;
            font-size: 0.9375rem;
            transition: all 0.2s;
        }

        .form-control:focus, .form-select:focus {
            outline: none;
            border-color: #8B5CF6;
            box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
        }

        .empty-state {
            text-align: center;
            padding: 3rem;
            color: #94A3B8;
        }

        .empty-state i {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: #CBD5E1;
        }

        @media (max-width: 768px) {
            .search-filter-group {
                grid-template-columns: 1fr;
            }

            .patients-table {
                font-size: 0.875rem;
            }

            .action-buttons {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="sidebar-overlay" id="sidebarOverlay"></div>

    <div class="modern-dashboard">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="sidebar-logo">
                    <i class="fas fa-heartbeat"></i>
                </div>
                <div>
                    <h2 class="sidebar-title">Bizil Health</h2>
                </div>
            </div>
            
            <nav>
                <ul class="nav-menu">
                    <li class="nav-item">
                        <a href="/admin/dashboard" class="nav-link">
                            <i class="fas fa-home nav-icon"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/patients" class="nav-link active">
                            <i class="fas fa-users nav-icon"></i>
                            <span>Patients</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/doctors" class="nav-link">
                            <i class="fas fa-user-md nav-icon"></i>
                            <span>Doctors</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/appointments" class="nav-link">
                            <i class="fas fa-calendar-check nav-icon"></i>
                            <span>Appointments</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/departments" class="nav-link">
                            <i class="fas fa-hospital nav-icon"></i>
                            <span>Departments</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/health-tips" class="nav-link">
                            <i class="fas fa-lightbulb nav-icon"></i>
                            <span>Health Tips</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/reports" class="nav-link">
                            <i class="fas fa-chart-line nav-icon"></i>
                            <span>Reports</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/admin/settings" class="nav-link">
                            <i class="fas fa-cog nav-icon"></i>
                            <span>Settings</span>
                        </a>
                    </li>
                    <li class="nav-item" style="margin-top: 2rem;">
                        <a href="/patient/logout" class="nav-link" style="color: #EF4444;">
                            <i class="fas fa-sign-out-alt nav-icon"></i>
                            <span>Logout</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <header class="dashboard-header">
                <div class="welcome-section">
                    <div style="display: flex; align-items: center; gap: 1rem;">
                        <button class="mobile-menu-toggle" id="mobileMenuToggle">
                            <i class="fas fa-bars"></i>
                        </button>
                        <div>
                            <h1 style="margin-bottom: 0.25rem;">Patients Management</h1>
                            <p style="margin: 0;">Manage and monitor all registered patients</p>
                        </div>
                    </div>
                </div>
                
                <div class="header-actions">
                    <div class="user-profile">
                        <div class="user-avatar" style="background: linear-gradient(135deg, #8B5CF6, #6366F1);">
                            <%= user && user.username ? user.username.charAt(0).toUpperCase() : 'A' %>
                        </div>
                        <div class="user-info">
                            <div class="user-name"><%= user && user.username ? user.username : 'Admin' %></div>
                            <div class="user-role">Administrator</div>
                        </div>
                    </div>
                </div>
            </header>

            <!-- Alerts -->
            <% if (successMessage) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle"></i> <%= successMessage %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>
            <% if (errorMessage) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="fas fa-exclamation-circle"></i> <%= errorMessage %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>

            <!-- Statistics -->
            <div class="stats-row">
                <div class="stat-box">
                    <h3><%= totalPatients %></h3>
                    <p>Total Patients</p>
                </div>
                <div class="stat-box">
                    <h3><%= maleCount %></h3>
                    <p>Male Patients</p>
                </div>
                <div class="stat-box">
                    <h3><%= femaleCount %></h3>
                    <p>Female Patients</p>
                </div>
            </div>

            <!-- Filters -->
            <div class="filter-section">
                <form method="GET" action="/admin/patients">
                    <div class="search-filter-group">
                        <div class="form-group">
                            <label>Search Patients</label>
                            <input type="text" name="search" class="form-control" 
                                   placeholder="Search by name, email, or phone..." 
                                   value="<%= filters.search || '' %>">
                        </div>
                        <div class="form-group">
                            <label>Gender</label>
                            <select name="gender" class="form-select">
                                <option value="all" <%= filters.gender === 'all' || !filters.gender ? 'selected' : '' %>>All Genders</option>
                                <option value="male" <%= filters.gender === 'male' ? 'selected' : '' %>>Male</option>
                                <option value="female" <%= filters.gender === 'female' ? 'selected' : '' %>>Female</option>
                                <option value="other" <%= filters.gender === 'other' ? 'selected' : '' %>>Other</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Sort By</label>
                            <select name="sortBy" class="form-select" onchange="this.form.submit()">
                                <option value="newest" <%= filters.sortBy === 'newest' || !filters.sortBy ? 'selected' : '' %>>Newest First</option>
                                <option value="oldest" <%= filters.sortBy === 'oldest' ? 'selected' : '' %>>Oldest First</option>
                                <option value="name" <%= filters.sortBy === 'name' ? 'selected' : '' %>>Name (A-Z)</option>
                            </select>
                        </div>
                    </div>
                    <button type="submit" class="btn-primary" style="margin-top: 1rem;">
                        <i class="fas fa-search"></i> Apply Filters
                    </button>
                </form>
            </div>

            <!-- Patients Table -->
            <div class="patients-table-container">
                <div class="table-header">
                    <h2><i class="fas fa-users"></i> All Patients (<%= patients.length %>)</h2>
                </div>

                <% if (patients && patients.length > 0) { %>
                    <table class="patients-table">
                        <thead>
                            <tr>
                                <th>Patient</th>
                                <th>Contact</th>
                                <th>Gender</th>
                                <th>Date of Birth</th>
                                <th>Registered</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% patients.forEach(patient => { %>
                                <tr>
                                    <td>
                                        <div style="display: flex; align-items: center;">
                                            <div class="patient-avatar">
                                                <%= patient.firstName.charAt(0).toUpperCase() %><%= patient.lastName.charAt(0).toUpperCase() %>
                                            </div>
                                            <div class="patient-info">
                                                <span class="patient-name"><%= patient.firstName %> <%= patient.lastName %></span>
                                                <span class="patient-email"><%= patient.user?.username || 'N/A' %></span>
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        <div><%= patient.phone || 'N/A' %></div>
                                        <div class="patient-email"><%= patient.user?.email || 'N/A' %></div>
                                    </td>
                                    <td>
                                        <span class="badge badge-<%= patient.gender %>">
                                            <%= patient.gender.charAt(0).toUpperCase() + patient.gender.slice(1) %>
                                        </span>
                                    </td>
                                    <td><%= patient.dateOfBirth ? new Date(patient.dateOfBirth).toLocaleDateString() : 'N/A' %></td>
                                    <td><%= patient.createdAt ? new Date(patient.createdAt).toLocaleDateString() : 'N/A' %></td>
                                    <td>
                                        <div class="action-buttons">
                                            <a href="/admin/patients/view/<%= patient._id %>" class="btn-action btn-view">
                                                <i class="fas fa-eye"></i> View
                                            </a>
                                            <form method="POST" action="/admin/patients/delete/<%= patient._id %>" style="display: inline;" 
                                                  onsubmit="return confirm('Are you sure you want to delete this patient? This action cannot be undone.');">
                                                <button type="submit" class="btn-action btn-delete">
                                                    <i class="fas fa-trash"></i> Delete
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                <% } else { %>
                    <div class="empty-state">
                        <i class="fas fa-user-slash"></i>
                        <p>No patients found</p>
                    </div>
                <% } %>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Mobile menu toggle
        const mobileMenuToggle = document.getElementById('mobileMenuToggle');
        const sidebar = document.querySelector('.sidebar');
        const overlay = document.getElementById('sidebarOverlay');

        if (mobileMenuToggle) {
            mobileMenuToggle.addEventListener('click', function() {
                sidebar.classList.toggle('active');
                overlay.classList.toggle('active');
            });
        }

        if (overlay) {
            overlay.addEventListener('click', function() {
                sidebar.classList.remove('active');
                overlay.classList.remove('active');
            });
        }
    </script>
</body>
</html>
