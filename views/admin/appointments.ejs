<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Appointments Management - Bizil Health Care Admin</title>
    <link rel="icon" type="image/png" href="/images/favicon.ico">
    <link rel="shortcut icon" type="image/png" href="/images/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" crossorigin="anonymous" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/modern-dashboard.css">
    <style>
        body { background: #F8FAFC; font-family: 'Inter', sans-serif; }
        .filter-section, .appointments-container { background: white; border-radius: 1rem; padding: 1.5rem; margin-bottom: 1.5rem; box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1); border: 1px solid #E2E8F0; }
        .stats-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 1rem; margin-bottom: 1.5rem; }
        .stat-box { background: white; border-radius: 1rem; padding: 1.25rem; box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1); border: 1px solid #E2E8F0; text-align: center; }
        .stat-box h3 { font-size: 2rem; font-weight: 700; color: #1E293B; margin-bottom: 0.25rem; }
        .stat-box p { font-size: 0.875rem; color: #64748B; margin: 0; }
        .appointment-card { background: #F8FAFC; border-radius: 0.75rem; padding: 1rem; margin-bottom: 0.75rem; transition: all 0.2s; }
        .appointment-card:hover { background: #F1F5F9; }
        .badge { padding: 0.375rem 0.75rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; display: inline-block; }
        .badge-scheduled { background: rgba(59, 130, 246, 0.1); color: #3B82F6; }
        .badge-confirmed { background: rgba(16, 185, 129, 0.1); color: #10B981; }
        .badge-completed { background: rgba(139, 92, 246, 0.1); color: #8B5CF6; }
        .badge-cancelled { background: rgba(239, 68, 68, 0.1); color: #EF4444; }
        .btn-action { padding: 0.5rem 0.75rem; border-radius: 0.5rem; border: none; font-size: 0.875rem; font-weight: 500; cursor: pointer; transition: all 0.2s; }
        .btn-update { background: rgba(59, 130, 246, 0.1); color: #3B82F6; }
        .btn-update:hover { background: rgba(59, 130, 246, 0.2); }
        .btn-delete { background: rgba(239, 68, 68, 0.1); color: #EF4444; }
        .btn-delete:hover { background: rgba(239, 68, 68, 0.2); }
    </style>
</head>
<body>
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    <div class="modern-dashboard">
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="sidebar-logo"><i class="fas fa-heartbeat"></i></div>
                <div><h2 class="sidebar-title">Bizil Health</h2></div>
            </div>
            <nav>
                <ul class="nav-menu">
                    <li class="nav-item"><a href="/admin/dashboard" class="nav-link"><i class="fas fa-home nav-icon"></i><span>Dashboard</span></a></li>
                    <li class="nav-item"><a href="/admin/patients" class="nav-link"><i class="fas fa-users nav-icon"></i><span>Patients</span></a></li>
                    <li class="nav-item"><a href="/admin/doctors" class="nav-link"><i class="fas fa-user-md nav-icon"></i><span>Doctors</span></a></li>
                    <li class="nav-item"><a href="/admin/appointments" class="nav-link active"><i class="fas fa-calendar-check nav-icon"></i><span>Appointments</span></a></li>
                    <li class="nav-item"><a href="/admin/departments" class="nav-link"><i class="fas fa-hospital nav-icon"></i><span>Departments</span></a></li>
                    <li class="nav-item"><a href="/admin/health-tips" class="nav-link"><i class="fas fa-lightbulb nav-icon"></i><span>Health Tips</span></a></li>
                    <li class="nav-item"><a href="/admin/reports" class="nav-link"><i class="fas fa-chart-line nav-icon"></i><span>Reports</span></a></li>
                    <li class="nav-item"><a href="/admin/settings" class="nav-link"><i class="fas fa-cog nav-icon"></i><span>Settings</span></a></li>
                    <li class="nav-item" style="margin-top: 2rem;"><a href="/patient/logout" class="nav-link" style="color: #EF4444;"><i class="fas fa-sign-out-alt nav-icon"></i><span>Logout</span></a></li>
                </ul>
            </nav>
        </aside>

        <main class="main-content">
            <header class="dashboard-header">
                <div class="welcome-section">
                    <div style="display: flex; align-items: center; gap: 1rem;">
                        <button class="mobile-menu-toggle" id="mobileMenuToggle"><i class="fas fa-bars"></i></button>
                        <div>
                            <h1 style="margin-bottom: 0.25rem;">Appointments Management</h1>
                            <p style="margin: 0;">Manage and monitor all appointments</p>
                        </div>
                    </div>
                </div>
                <div class="header-actions">
                    <div class="user-profile">
                        <div class="user-avatar" style="background: linear-gradient(135deg, #8B5CF6, #6366F1);"><%= user && user.username ? user.username.charAt(0).toUpperCase() : 'A' %></div>
                        <div class="user-info">
                            <div class="user-name"><%= user && user.username ? user.username : 'Admin' %></div>
                            <div class="user-role">Administrator</div>
                        </div>
                    </div>
                </div>
            </header>

            <% if (successMessage) { %><div class="alert alert-success alert-dismissible fade show"><i class="fas fa-check-circle"></i> <%= successMessage %><button type="button" class="btn-close" data-bs-dismiss="alert"></button></div><% } %>
            <% if (errorMessage) { %><div class="alert alert-danger alert-dismissible fade show"><i class="fas fa-exclamation-circle"></i> <%= errorMessage %><button type="button" class="btn-close" data-bs-dismiss="alert"></button></div><% } %>

            <div class="stats-row">
                <div class="stat-box"><h3><%= totalAppointments %></h3><p>Total Appointments</p></div>
                <div class="stat-box"><h3><%= scheduledCount %></h3><p>Scheduled</p></div>
                <div class="stat-box"><h3><%= confirmedCount %></h3><p>Confirmed</p></div>
                <div class="stat-box"><h3><%= completedCount %></h3><p>Completed</p></div>
                <div class="stat-box"><h3><%= cancelledCount %></h3><p>Cancelled</p></div>
            </div>

            <div class="filter-section">
                <form method="GET" action="/admin/appointments" style="display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 1rem;">
                    <div><label style="font-size: 0.875rem; font-weight: 600; color: #1E293B; display: block; margin-bottom: 0.5rem;">Search</label><input type="text" name="search" class="form-control" placeholder="Search patient or doctor..." value="<%= filters.search || '' %>"></div>
                    <div><label style="font-size: 0.875rem; font-weight: 600; color: #1E293B; display: block; margin-bottom: 0.5rem;">Status</label><select name="status" class="form-select"><option value="all">All Status</option><option value="scheduled" <%= filters.status === 'scheduled' ? 'selected' : '' %>>Scheduled</option><option value="confirmed" <%= filters.status === 'confirmed' ? 'selected' : '' %>>Confirmed</option><option value="completed" <%= filters.status === 'completed' ? 'selected' : '' %>>Completed</option><option value="cancelled" <%= filters.status === 'cancelled' ? 'selected' : '' %>>Cancelled</option></select></div>
                    <div><label style="font-size: 0.875rem; font-weight: 600; color: #1E293B; display: block; margin-bottom: 0.5rem;">Date</label><input type="date" name="date" class="form-control" value="<%= filters.date || '' %>"></div>
                    <div><label style="font-size: 0.875rem; font-weight: 600; color: #1E293B; display: block; margin-bottom: 0.5rem;">Sort By</label><select name="sortBy" class="form-select" onchange="this.form.submit()"><option value="date-desc">Newest First</option><option value="date-asc" <%= filters.sortBy === 'date-asc' ? 'selected' : '' %>>Oldest First</option><option value="status" <%= filters.sortBy === 'status' ? 'selected' : '' %>>Status</option></select></div>
                </form>
                <button type="submit" class="btn-primary" style="margin-top: 1rem;" onclick="this.previousElementSibling.submit()"><i class="fas fa-search"></i> Apply Filters</button>
            </div>

            <div class="appointments-container">
                <h2 style="font-size: 1.25rem; font-weight: 600; margin-bottom: 1.5rem;"><i class="fas fa-calendar-alt"></i> All Appointments (<%= appointments.length %>)</h2>
                <% if (appointments && appointments.length > 0) { %>
                    <% appointments.forEach(apt => { %>
                        <div class="appointment-card">
                            <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
                                <div style="flex: 1;">
                                    <div style="font-weight: 600; color: #1E293B; margin-bottom: 0.25rem;">
                                        <%= apt.patient ? (apt.patient.firstName + ' ' + apt.patient.lastName) : 'Patient N/A' %> 
                                        <i class="fas fa-arrow-right" style="color: #94A3B8; margin: 0 0.5rem;"></i>
                                        Dr. <%= apt.doctor ? (apt.doctor.firstName + ' ' + apt.doctor.lastName) : 'Doctor N/A' %>
                                    </div>
                                    <div style="font-size: 0.875rem; color: #64748B;">
                                        <i class="fas fa-calendar"></i> <%= apt.appointmentDate ? new Date(apt.appointmentDate).toLocaleDateString() : 'N/A' %>
                                        <i class="fas fa-clock" style="margin-left: 1rem;"></i> <%= apt.appointmentTime || 'N/A' %>
                                        <% if (apt.doctor && apt.doctor.specialization) { %>
                                            <i class="fas fa-stethoscope" style="margin-left: 1rem;"></i> <%= apt.doctor.specialization %>
                                        <% } %>
                                    </div>
                                </div>
                                <div style="display: flex; align-items: center; gap: 1rem;">
                                    <span class="badge badge-<%= apt.status %>"><%= apt.status.charAt(0).toUpperCase() + apt.status.slice(1) %></span>
                                    <div style="display: flex; gap: 0.5rem;">
                                        <form method="POST" action="/admin/appointments/update-status/<%= apt._id %>" style="display: inline;">
                                            <select name="status" class="form-select" style="font-size: 0.875rem; padding: 0.5rem;" onchange="this.form.submit()">
                                                <option value="scheduled" <%= apt.status === 'scheduled' ? 'selected' : '' %>>Scheduled</option>
                                                <option value="confirmed" <%= apt.status === 'confirmed' ? 'selected' : '' %>>Confirmed</option>
                                                <option value="completed" <%= apt.status === 'completed' ? 'selected' : '' %>>Completed</option>
                                                <option value="cancelled" <%= apt.status === 'cancelled' ? 'selected' : '' %>>Cancelled</option>
                                            </select>
                                        </form>
                                        <form method="POST" action="/admin/appointments/delete/<%= apt._id %>" onsubmit="return confirm('Delete this appointment?');" style="display: inline;">
                                            <button type="submit" class="btn-action btn-delete"><i class="fas fa-trash"></i></button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div style="text-align: center; padding: 3rem; color: #94A3B8;"><i class="fas fa-calendar-times" style="font-size: 3rem; margin-bottom: 1rem; display: block; color: #CBD5E1;"></i><p>No appointments found</p></div>
                <% } %>
            </div>
        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const mobileMenuToggle = document.getElementById('mobileMenuToggle');
        const sidebar = document.querySelector('.sidebar');
        const overlay = document.getElementById('sidebarOverlay');
        if (mobileMenuToggle) mobileMenuToggle.addEventListener('click', () => { sidebar.classList.toggle('active'); overlay.classList.toggle('active'); });
        if (overlay) overlay.addEventListener('click', () => { sidebar.classList.remove('active'); overlay.classList.remove('active'); });
    </script>
</body>
</html>
