<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doctor Details - Dr. <%= doctor.firstName %> <%= doctor.lastName %> - Admin</title>
    <link rel="icon" type="image/png" href="/images/favicon.ico">
    <link rel="shortcut icon" type="image/png" href="/images/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" crossorigin="anonymous" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/modern-dashboard.css">
    <style>
        body { background: #F8FAFC; font-family: 'Inter', sans-serif; }
        .back-button { display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.625rem 1.25rem; background: white; border: 1px solid #E2E8F0; border-radius: 0.5rem; color: #64748B; text-decoration: none; font-weight: 500; transition: all 0.2s; margin-bottom: 1.5rem; }
        .back-button:hover { background: #F8FAFC; color: #1E293B; border-color: #CBD5E1; }
        .doctor-header { background: white; border-radius: 1.5rem; padding: 2rem; margin-bottom: 1.5rem; box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1); border: 1px solid #E2E8F0; }
        .doctor-avatar-large { width: 80px; height: 80px; border-radius: 50%; background: linear-gradient(135deg, #3B82F6, #2563EB); display: flex; align-items: center; justify-content: center; color: white; font-size: 2rem; font-weight: 700; }
        .info-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem; margin-bottom: 1.5rem; }
        .info-card { background: white; border-radius: 1rem; padding: 1.5rem; box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1); border: 1px solid #E2E8F0; }
        .info-card h3 { font-size: 1.125rem; font-weight: 600; color: #1E293B; margin-bottom: 1.25rem; display: flex; align-items: center; gap: 0.5rem; }
        .info-row { display: flex; justify-content: space-between; padding: 0.75rem 0; border-bottom: 1px solid #F1F5F9; }
        .info-row:last-child { border-bottom: none; }
        .info-label { color: #64748B; font-weight: 500; font-size: 0.9375rem; }
        .info-value { color: #1E293B; font-weight: 600; font-size: 0.9375rem; text-align: right; }
        .section-card { background: white; border-radius: 1rem; padding: 1.5rem; margin-bottom: 1.5rem; box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1); border: 1px solid #E2E8F0; }
        .section-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.25rem; padding-bottom: 1rem; border-bottom: 1px solid #E2E8F0; }
        .section-header h2 { font-size: 1.25rem; font-weight: 600; color: #1E293B; margin: 0; display: flex; align-items: center; gap: 0.5rem; }
        .appointment-item { background: #F8FAFC; border-radius: 0.75rem; padding: 1rem; margin-bottom: 0.75rem; transition: all 0.2s; }
        .appointment-item:hover { background: #F1F5F9; }
        .badge { padding: 0.375rem 0.75rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; display: inline-block; }
        .badge-verified { background: rgba(16, 185, 129, 0.1); color: #10B981; }
        .badge-pending { background: rgba(245, 158, 11, 0.1); color: #F59E0B; }
        .badge-scheduled { background: rgba(59, 130, 246, 0.1); color: #3B82F6; }
        .badge-confirmed { background: rgba(16, 185, 129, 0.1); color: #10B981; }
        .badge-completed { background: rgba(139, 92, 246, 0.1); color: #8B5CF6; }
        .badge-cancelled { background: rgba(239, 68, 68, 0.1); color: #EF4444; }
        .stat-box { background: linear-gradient(135deg, rgba(59, 130, 246, 0.05), rgba(37, 99, 235, 0.05)); border-radius: 1rem; padding: 1.5rem; text-align: center; border: 1px solid rgba(59, 130, 246, 0.1); }
        .stat-value { font-size: 2rem; font-weight: 700; color: #3B82F6; margin-bottom: 0.25rem; }
        .stat-label { font-size: 0.875rem; color: #64748B; }
        .empty-state { text-align: center; padding: 2rem; color: #94A3B8; }
        .empty-state i { font-size: 2rem; margin-bottom: 0.5rem; color: #CBD5E1; }
        .rating-stars { color: #F59E0B; }
    </style>
</head>
<body>
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    
    <div class="modern-dashboard">
        <aside class="sidebar">
            <div class="sidebar-header"><div class="sidebar-logo"><i class="fas fa-heartbeat"></i></div><div><h2 class="sidebar-title">Bizil Health</h2></div></div>
            <nav><ul class="nav-menu">
                <li class="nav-item"><a href="/admin/dashboard" class="nav-link"><i class="fas fa-home nav-icon"></i><span>Dashboard</span></a></li>
                <li class="nav-item"><a href="/admin/patients" class="nav-link"><i class="fas fa-users nav-icon"></i><span>Patients</span></a></li>
                <li class="nav-item"><a href="/admin/doctors" class="nav-link active"><i class="fas fa-user-md nav-icon"></i><span>Doctors</span></a></li>
                <li class="nav-item"><a href="/admin/appointments" class="nav-link"><i class="fas fa-calendar-check nav-icon"></i><span>Appointments</span></a></li>
                <li class="nav-item"><a href="/admin/departments" class="nav-link"><i class="fas fa-hospital nav-icon"></i><span>Departments</span></a></li>
                <li class="nav-item"><a href="/admin/health-tips" class="nav-link"><i class="fas fa-lightbulb nav-icon"></i><span>Health Tips</span></a></li>
                <li class="nav-item"><a href="/admin/reports" class="nav-link"><i class="fas fa-chart-line nav-icon"></i><span>Reports</span></a></li>
                <li class="nav-item"><a href="/admin/settings" class="nav-link"><i class="fas fa-cog nav-icon"></i><span>Settings</span></a></li>
                <li class="nav-item" style="margin-top: 2rem;"><a href="/patient/logout" class="nav-link" style="color: #EF4444;"><i class="fas fa-sign-out-alt nav-icon"></i><span>Logout</span></a></li>
            </ul></nav>
        </aside>

        <main class="main-content">
            <header class="dashboard-header">
                <div class="welcome-section"><div style="display: flex; align-items: center; gap: 1rem;"><button class="mobile-menu-toggle" id="mobileMenuToggle"><i class="fas fa-bars"></i></button><div><h1 style="margin-bottom: 0.25rem;">Doctor Details</h1><p style="margin: 0;">Complete doctor information and statistics</p></div></div></div>
                <div class="header-actions"><div class="user-profile"><div class="user-avatar" style="background: linear-gradient(135deg, #8B5CF6, #6366F1);"><%= user.username.charAt(0).toUpperCase() %></div><div class="user-info"><div class="user-name"><%= user.username %></div><div class="user-role">Administrator</div></div></div></div>
            </header>

            <% if (successMessage) { %><div class="alert alert-success alert-dismissible fade show"><i class="fas fa-check-circle"></i> <%= successMessage %><button type="button" class="btn-close" data-bs-dismiss="alert"></button></div><% } %>
            <% if (errorMessage) { %><div class="alert alert-danger alert-dismissible fade show"><i class="fas fa-exclamation-circle"></i> <%= errorMessage %><button type="button" class="btn-close" data-bs-dismiss="alert"></button></div><% } %>

            <a href="/admin/doctors" class="back-button"><i class="fas fa-arrow-left"></i> Back to Doctors</a>

            <!-- Doctor Header -->
            <div class="doctor-header">
                <div style="display: flex; align-items: center; gap: 2rem; flex-wrap: wrap;">
                    <div class="doctor-avatar-large"><%= doctor.firstName.charAt(0).toUpperCase() %><%= doctor.lastName.charAt(0).toUpperCase() %></div>
                    <div style="flex: 1;">
                        <h2 style="font-size: 2rem; font-weight: 700; color: #1E293B; margin-bottom: 0.5rem;">Dr. <%= doctor.firstName %> <%= doctor.lastName %></h2>
                        <div style="display: flex; gap: 1.5rem; flex-wrap: wrap; color: #64748B; font-size: 0.9375rem; margin-bottom: 0.75rem;">
                            <span><i class="fas fa-stethoscope" style="margin-right: 0.5rem;"></i> <%= doctor.specialization || 'General' %></span>
                            <span>
                                <i class="fas fa-hospital" style="margin-right: 0.5rem;"></i> 
                                <% if (doctor.currentHospital) { %>
                                    <% if (typeof doctor.currentHospital === 'object') { %>
                                        <%= doctor.currentHospital.name || 'N/A' %>
                                    <% } else { %>
                                        <%= doctor.currentHospital %>
                                    <% } %>
                                <% } else { %>
                                    N/A
                                <% } %>
                            </span>
                            <span><i class="fas fa-briefcase" style="margin-right: 0.5rem;"></i> <%= doctor.experience || 0 %> years experience</span>
                        </div>
                        <div style="display: flex; gap: 1rem; align-items: center; flex-wrap: wrap;">
                            <span class="badge badge-<%= doctor.isVerified ? 'verified' : 'pending' %>">
                                <%= doctor.isVerified ? 'Verified' : 'Pending Verification' %>
                            </span>
                            <% if (doctor.ratings && doctor.ratings.overall) { %>
                                <span class="rating-stars">
                                    <% for (let i = 1; i <= 5; i++) { %>
                                        <i class="fas fa-star<%= i <= doctor.ratings.overall ? '' : '-half-alt' %>"></i>
                                    <% } %>
                                    <%= doctor.ratings.overall.toFixed(1) %>
                                </span>
                            <% } %>
                        </div>
                    </div>
                    <div style="display: flex; gap: 0.75rem;">
                        <form method="POST" action="/admin/doctors/toggle-verify/<%= doctor._id %>">
                            <button type="submit" class="btn-primary" style="<%= doctor.isVerified ? 'background: rgba(245, 158, 11, 0.9);' : '' %>">
                                <i class="fas <%= doctor.isVerified ? 'fa-times-circle' : 'fa-check-circle' %>"></i>
                                <%= doctor.isVerified ? 'Unverify' : 'Verify' %>
                            </button>
                        </form>
                        <form method="POST" action="/admin/doctors/delete/<%= doctor._id %>" onsubmit="return confirm('Delete this doctor?');">
                            <button type="submit" class="btn-secondary" style="background: rgba(239, 68, 68, 0.1); color: #EF4444; border: 1px solid rgba(239, 68, 68, 0.2);">
                                <i class="fas fa-trash"></i> Delete
                            </button>
                        </form>
                    </div>
                </div>
            </div>

            <!-- Statistics -->
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1.5rem; margin-bottom: 1.5rem;">
                <div class="stat-box">
                    <div class="stat-value"><%= totalAppointments %></div>
                    <div class="stat-label">Total Appointments</div>
                </div>
                <div class="stat-box">
                    <div class="stat-value"><%= completedAppointments %></div>
                    <div class="stat-label">Completed</div>
                </div>
                <div class="stat-box">
                    <div class="stat-value">₹<%= doctor.consultationFee || 0 %></div>
                    <div class="stat-label">Consultation Fee</div>
                </div>
            </div>

            <!-- Information Grid -->
            <div class="info-grid">
                <div class="info-card">
                    <h3><i class="fas fa-id-card" style="color: #3B82F6;"></i> Professional Info</h3>
                    <div class="info-row"><span class="info-label">Medical License</span><span class="info-value"><%= doctor.medicalLicenseNumber || 'N/A' %></span></div>
                    <div class="info-row"><span class="info-label">Specialization</span><span class="info-value"><%= doctor.specialization || 'N/A' %></span></div>
                    <div class="info-row"><span class="info-label">Experience</span><span class="info-value"><%= doctor.experience || 0 %> years</span></div>
                    <div class="info-row"><span class="info-label">Qualification</span><span class="info-value"><%= doctor.qualification || 'N/A' %></span></div>
                </div>

                <div class="info-card">
                    <h3><i class="fas fa-address-book" style="color: #10B981;"></i> Contact Info</h3>
                    <div class="info-row"><span class="info-label">Phone</span><span class="info-value"><%= doctor.phone || 'N/A' %></span></div>
                    <div class="info-row"><span class="info-label">Email</span><span class="info-value"><%= doctor.user?.email || 'N/A' %></span></div>
                    <div class="info-row">
                        <span class="info-label">Hospital</span>
                        <span class="info-value">
                            <% if (doctor.currentHospital) { %>
                                <% if (typeof doctor.currentHospital === 'object') { %>
                                    <%= doctor.currentHospital.name || 'N/A' %>
                                <% } else { %>
                                    <%= doctor.currentHospital %>
                                <% } %>
                            <% } else { %>
                                N/A
                            <% } %>
                        </span>
                    </div>
                </div>

                <div class="info-card">
                    <h3><i class="fas fa-calendar-alt" style="color: #F59E0B;"></i> Availability</h3>
                    <% if (doctor.availability && doctor.availability.length > 0) { %>
                        <% doctor.availability.forEach(slot => { %>
                            <div class="info-row"><span class="info-label"><%= slot.day %></span><span class="info-value"><%= slot.startTime %> - <%= slot.endTime %></span></div>
                        <% }); %>
                    <% } else { %>
                        <div class="info-row"><span class="info-label">Schedule</span><span class="info-value">Not set</span></div>
                    <% } %>
                </div>
            </div>

            <!-- Appointments -->
            <div class="section-card">
                <div class="section-header">
                    <h2><i class="fas fa-calendar-check" style="color: #3B82F6;"></i> Recent Appointments</h2>
                    <span style="color: #64748B; font-size: 0.9375rem;"><%= appointments.length %> appointments</span>
                </div>
                
                <% if (appointments && appointments.length > 0) { %>
                    <% appointments.forEach(apt => { %>
                        <div class="appointment-item">
                            <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
                                <div>
                                    <div style="font-weight: 600; color: #1E293B; margin-bottom: 0.25rem;">
                                        <%= apt.patient ? (apt.patient.firstName + ' ' + apt.patient.lastName) : 'Patient N/A' %>
                                    </div>
                                    <div style="font-size: 0.875rem; color: #64748B;">
                                        <i class="fas fa-calendar"></i> <%= apt.appointmentDate ? new Date(apt.appointmentDate).toLocaleDateString() : 'N/A' %>
                                        <i class="fas fa-clock" style="margin-left: 1rem;"></i> <%= apt.appointmentTime || 'N/A' %>
                                        <% if (apt.patient && apt.patient.phone) { %>
                                            <i class="fas fa-phone" style="margin-left: 1rem;"></i> <%= apt.patient.phone %>
                                        <% } %>
                                    </div>
                                </div>
                                <span class="badge badge-<%= apt.status %>"><%= apt.status.charAt(0).toUpperCase() + apt.status.slice(1) %></span>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state"><i class="fas fa-calendar-times"></i><p>No appointments found</p></div>
                <% } %>
            </div>

        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const mobileMenuToggle = document.getElementById('mobileMenuToggle');
        const sidebar = document.querySelector('.sidebar');
        const overlay = document.getElementById('sidebarOverlay');
        if (mobileMenuToggle) mobileMenuToggle.addEventListener('click', () => { sidebar.classList.toggle('active'); overlay.classList.toggle('active'); });
        if (overlay) overlay.addEventListener('click', () => { sidebar.classList.remove('active'); overlay.classList.remove('active'); });
    </script>
</body>
</html>
