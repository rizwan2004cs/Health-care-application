<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patient Details - <%= patient.firstName %> <%= patient.lastName %> - Admin</title>
    <link rel="icon" type="image/png" href="/images/favicon.ico">
    <link rel="shortcut icon" type="image/png" href="/images/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" crossorigin="anonymous" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/modern-dashboard.css">
    <style>
        body { background: #F8FAFC; font-family: 'Inter', sans-serif; }
        
        .back-button {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.625rem 1.25rem;
            background: white;
            border: 1px solid #E2E8F0;
            border-radius: 0.5rem;
            color: #64748B;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.2s;
            margin-bottom: 1.5rem;
        }
        
        .back-button:hover {
            background: #F8FAFC;
            color: #1E293B;
            border-color: #CBD5E1;
        }
        
        .patient-header {
            background: white;
            border-radius: 1.5rem;
            padding: 2rem;
            margin-bottom: 1.5rem;
            box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1);
            border: 1px solid #E2E8F0;
        }
        
        .patient-avatar-large {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: linear-gradient(135deg, #8B5CF6, #6366F1);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 2rem;
            font-weight: 700;
        }
        
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-top: 1.5rem;
        }
        
        .info-card {
            background: white;
            border-radius: 1rem;
            padding: 1.5rem;
            box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1);
            border: 1px solid #E2E8F0;
        }
        
        .info-card h3 {
            font-size: 1.125rem;
            font-weight: 600;
            color: #1E293B;
            margin-bottom: 1.25rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .info-row {
            display: flex;
            justify-content: space-between;
            padding: 0.75rem 0;
            border-bottom: 1px solid #F1F5F9;
        }
        
        .info-row:last-child {
            border-bottom: none;
        }
        
        .info-label {
            color: #64748B;
            font-weight: 500;
            font-size: 0.9375rem;
        }
        
        .info-value {
            color: #1E293B;
            font-weight: 600;
            font-size: 0.9375rem;
        }
        
        .section-card {
            background: white;
            border-radius: 1rem;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1);
            border: 1px solid #E2E8F0;
        }
        
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.25rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #E2E8F0;
        }
        
        .section-header h2 {
            font-size: 1.25rem;
            font-weight: 600;
            color: #1E293B;
            margin: 0;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .appointment-item, .prescription-item, .test-item {
            background: #F8FAFC;
            border-radius: 0.75rem;
            padding: 1rem;
            margin-bottom: 0.75rem;
            transition: all 0.2s;
        }
        
        .appointment-item:hover, .prescription-item:hover, .test-item:hover {
            background: #F1F5F9;
        }
        
        .badge {
            padding: 0.375rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 600;
            display: inline-block;
        }
        
        .badge-male { background: rgba(59, 130, 246, 0.1); color: #3B82F6; }
        .badge-female { background: rgba(236, 72, 153, 0.1); color: #EC4899; }
        .badge-scheduled { background: rgba(59, 130, 246, 0.1); color: #3B82F6; }
        .badge-confirmed { background: rgba(16, 185, 129, 0.1); color: #10B981; }
        .badge-completed { background: rgba(139, 92, 246, 0.1); color: #8B5CF6; }
        .badge-cancelled { background: rgba(239, 68, 68, 0.1); color: #EF4444; }
        
        .empty-state {
            text-align: center;
            padding: 2rem;
            color: #94A3B8;
        }
        
        .empty-state i {
            font-size: 2rem;
            margin-bottom: 0.5rem;
            color: #CBD5E1;
        }
        
        .vitals-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1rem;
            margin-top: 1rem;
        }
        
        .vital-box {
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.05), rgba(99, 102, 241, 0.05));
            border-radius: 0.75rem;
            padding: 1rem;
            text-align: center;
            border: 1px solid rgba(139, 92, 246, 0.1);
        }
        
        .vital-value {
            font-size: 1.5rem;
            font-weight: 700;
            color: #8B5CF6;
            margin-bottom: 0.25rem;
        }
        
        .vital-label {
            font-size: 0.875rem;
            color: #64748B;
        }
    </style>
</head>
<body>
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    
    <div class="modern-dashboard">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="sidebar-logo"><i class="fas fa-heartbeat"></i></div>
                <div><h2 class="sidebar-title">Bizil Health</h2></div>
            </div>
            <nav>
                <ul class="nav-menu">
                    <li class="nav-item"><a href="/admin/dashboard" class="nav-link"><i class="fas fa-home nav-icon"></i><span>Dashboard</span></a></li>
                    <li class="nav-item"><a href="/admin/patients" class="nav-link active"><i class="fas fa-users nav-icon"></i><span>Patients</span></a></li>
                    <li class="nav-item"><a href="/admin/doctors" class="nav-link"><i class="fas fa-user-md nav-icon"></i><span>Doctors</span></a></li>
                    <li class="nav-item"><a href="/admin/appointments" class="nav-link"><i class="fas fa-calendar-check nav-icon"></i><span>Appointments</span></a></li>
                    <li class="nav-item"><a href="/admin/departments" class="nav-link"><i class="fas fa-hospital nav-icon"></i><span>Departments</span></a></li>
                    <li class="nav-item"><a href="/admin/health-tips" class="nav-link"><i class="fas fa-lightbulb nav-icon"></i><span>Health Tips</span></a></li>
                    <li class="nav-item"><a href="/admin/reports" class="nav-link"><i class="fas fa-chart-line nav-icon"></i><span>Reports</span></a></li>
                    <li class="nav-item"><a href="/admin/settings" class="nav-link"><i class="fas fa-cog nav-icon"></i><span>Settings</span></a></li>
                    <li class="nav-item" style="margin-top: 2rem;"><a href="/patient/logout" class="nav-link" style="color: #EF4444;"><i class="fas fa-sign-out-alt nav-icon"></i><span>Logout</span></a></li>
                </ul>
            </nav>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <header class="dashboard-header">
                <div class="welcome-section">
                    <div style="display: flex; align-items: center; gap: 1rem;">
                        <button class="mobile-menu-toggle" id="mobileMenuToggle"><i class="fas fa-bars"></i></button>
                        <div>
                            <h1 style="margin-bottom: 0.25rem;">Patient Details</h1>
                            <p style="margin: 0;">Complete patient information and history</p>
                        </div>
                    </div>
                </div>
                <div class="header-actions">
                    <div class="user-profile">
                        <div class="user-avatar" style="background: linear-gradient(135deg, #8B5CF6, #6366F1);"><%= user.username.charAt(0).toUpperCase() %></div>
                        <div class="user-info">
                            <div class="user-name"><%= user.username %></div>
                            <div class="user-role">Administrator</div>
                        </div>
                    </div>
                </div>
            </header>

            <!-- Alerts -->
            <% if (successMessage) { %>
                <div class="alert alert-success alert-dismissible fade show"><i class="fas fa-check-circle"></i> <%= successMessage %><button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
            <% } %>
            <% if (errorMessage) { %>
                <div class="alert alert-danger alert-dismissible fade show"><i class="fas fa-exclamation-circle"></i> <%= errorMessage %><button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>
            <% } %>

            <!-- Back Button -->
            <a href="/admin/patients" class="back-button">
                <i class="fas fa-arrow-left"></i> Back to Patients
            </a>

            <!-- Patient Header Card -->
            <div class="patient-header">
                <div style="display: flex; align-items: center; gap: 2rem; flex-wrap: wrap;">
                    <div class="patient-avatar-large">
                        <%= patient.firstName.charAt(0).toUpperCase() %><%= patient.lastName.charAt(0).toUpperCase() %>
                    </div>
                    <div style="flex: 1;">
                        <h2 style="font-size: 2rem; font-weight: 700; color: #1E293B; margin-bottom: 0.5rem;">
                            <%= patient.firstName %> <%= patient.lastName %>
                        </h2>
                        <div style="display: flex; gap: 1.5rem; flex-wrap: wrap; color: #64748B; font-size: 0.9375rem;">
                            <span><i class="fas fa-user" style="margin-right: 0.5rem;"></i> <%= patient.user?.username || 'N/A' %></span>
                            <span><i class="fas fa-envelope" style="margin-right: 0.5rem;"></i> <%= patient.user?.email || 'N/A' %></span>
                            <span><i class="fas fa-phone" style="margin-right: 0.5rem;"></i> <%= patient.phone || 'N/A' %></span>
                            <span class="badge badge-<%= patient.gender %>">
                                <%= patient.gender.charAt(0).toUpperCase() + patient.gender.slice(1) %>
                            </span>
                        </div>
                    </div>
                    <div>
                        <form method="POST" action="/admin/patients/delete/<%= patient._id %>" onsubmit="return confirm('Are you sure you want to delete this patient? This action cannot be undone.');" style="display: inline;">
                            <button type="submit" class="btn-secondary" style="background: rgba(239, 68, 68, 0.1); color: #EF4444; border: 1px solid rgba(239, 68, 68, 0.2);">
                                <i class="fas fa-trash"></i> Delete Patient
                            </button>
                        </form>
                    </div>
                </div>
            </div>

            <!-- Information Grid -->
            <div class="info-grid">
                <!-- Basic Information -->
                <div class="info-card">
                    <h3><i class="fas fa-id-card" style="color: #8B5CF6;"></i> Basic Information</h3>
                    <div class="info-row">
                        <span class="info-label">Date of Birth</span>
                        <span class="info-value"><%= patient.dateOfBirth ? new Date(patient.dateOfBirth).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) : 'N/A' %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Age</span>
                        <span class="info-value">
                            <% if (patient.dateOfBirth) { %>
                                <%= Math.floor((new Date() - new Date(patient.dateOfBirth)) / (365.25 * 24 * 60 * 60 * 1000)) %> years
                            <% } else { %>
                                N/A
                            <% } %>
                        </span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Gender</span>
                        <span class="info-value"><%= patient.gender.charAt(0).toUpperCase() + patient.gender.slice(1) %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Registered</span>
                        <span class="info-value"><%= patient.createdAt ? new Date(patient.createdAt).toLocaleDateString() : 'N/A' %></span>
                    </div>
                </div>

                <!-- Contact Information -->
                <div class="info-card">
                    <h3><i class="fas fa-address-book" style="color: #3B82F6;"></i> Contact Information</h3>
                    <div class="info-row">
                        <span class="info-label">Phone</span>
                        <span class="info-value"><%= patient.phone || 'N/A' %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Email</span>
                        <span class="info-value"><%= patient.user?.email || 'N/A' %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Address</span>
                        <span class="info-value">
                            <% if (patient.address) { %>
                                <% if (typeof patient.address === 'object') { %>
                                    <%= patient.address.street ? patient.address.street + ', ' : '' %>
                                    <%= patient.address.city ? patient.address.city + ', ' : '' %>
                                    <%= patient.address.state ? patient.address.state + ', ' : '' %>
                                    <%= patient.address.country || '' %>
                                <% } else { %>
                                    <%= patient.address %>
                                <% } %>
                            <% } else { %>
                                N/A
                            <% } %>
                        </span>
                    </div>
                </div>

                <!-- Emergency Contact -->
                <div class="info-card">
                    <h3><i class="fas fa-phone-square" style="color: #EF4444;"></i> Emergency Contact</h3>
                    <div class="info-row">
                        <span class="info-label">Name</span>
                        <span class="info-value"><%= patient.healthProfile?.emergencyContact?.name || 'N/A' %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Relationship</span>
                        <span class="info-value"><%= patient.healthProfile?.emergencyContact?.relationship || 'N/A' %></span>
                    </div>
                    <div class="info-row">
                        <span class="info-label">Phone</span>
                        <span class="info-value"><%= patient.healthProfile?.emergencyContact?.phone || 'N/A' %></span>
                    </div>
                </div>
            </div>

            <!-- Health Profile -->
            <div class="section-card">
                <div class="section-header">
                    <h2><i class="fas fa-heartbeat" style="color: #10B981;"></i> Health Profile</h2>
                </div>
                
                <div class="info-row">
                    <span class="info-label">Blood Type</span>
                    <span class="info-value"><%= patient.healthProfile?.bloodType || 'N/A' %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Height</span>
                    <span class="info-value">
                        <%= patient.healthProfile?.height?.value || 'N/A' %> 
                        <%= patient.healthProfile?.height?.unit || '' %>
                    </span>
                </div>
                <div class="info-row">
                    <span class="info-label">Weight</span>
                    <span class="info-value">
                        <%= patient.healthProfile?.weight?.value || 'N/A' %> 
                        <%= patient.healthProfile?.weight?.unit || '' %>
                    </span>
                </div>
                <div class="info-row">
                    <span class="info-label">Allergies</span>
                    <span class="info-value">
                        <% if (patient.healthProfile?.allergies && patient.healthProfile.allergies.length > 0) { %>
                            <%= patient.healthProfile.allergies.join(', ') %>
                        <% } else { %>
                            None reported
                        <% } %>
                    </span>
                </div>
                <div class="info-row">
                    <span class="info-label">Chronic Conditions</span>
                    <span class="info-value">
                        <% if (patient.healthProfile?.chronicConditions && patient.healthProfile.chronicConditions.length > 0) { %>
                            <%= patient.healthProfile.chronicConditions.join(', ') %>
                        <% } else { %>
                            None reported
                        <% } %>
                    </span>
                </div>

                <% if (patient.healthProfile?.vitalSigns) { %>
                    <h4 style="font-size: 1rem; font-weight: 600; color: #1E293B; margin-top: 1.5rem; margin-bottom: 1rem;">
                        <i class="fas fa-stethoscope"></i> Vital Signs
                    </h4>
                    <div class="vitals-grid">
                        <div class="vital-box">
                            <div class="vital-value"><%= patient.healthProfile.vitalSigns.heartRate || '--' %></div>
                            <div class="vital-label">Heart Rate (bpm)</div>
                        </div>
                        <div class="vital-box">
                            <div class="vital-value">
                                <%= patient.healthProfile.vitalSigns.bloodPressure?.systolic || '--' %>/<%= patient.healthProfile.vitalSigns.bloodPressure?.diastolic || '--' %>
                            </div>
                            <div class="vital-label">Blood Pressure</div>
                        </div>
                        <div class="vital-box">
                            <div class="vital-value"><%= patient.healthProfile.vitalSigns.oxygenSaturation || '--' %>%</div>
                            <div class="vital-label">Oxygen Level</div>
                        </div>
                        <div class="vital-box">
                            <div class="vital-value"><%= patient.healthProfile.vitalSigns.temperature || '--' %>°</div>
                            <div class="vital-label">Temperature</div>
                        </div>
                    </div>
                <% } %>
            </div>

            <!-- Appointments History -->
            <div class="section-card">
                <div class="section-header">
                    <h2><i class="fas fa-calendar-alt" style="color: #3B82F6;"></i> Appointment History</h2>
                    <span style="color: #64748B; font-size: 0.9375rem;"><%= appointments.length %> appointments</span>
                </div>
                
                <% if (appointments && appointments.length > 0) { %>
                    <% appointments.forEach(apt => { %>
                        <div class="appointment-item">
                            <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;">
                                <div>
                                    <div style="font-weight: 600; color: #1E293B; margin-bottom: 0.25rem;">
                                        Dr. <%= apt.doctor ? (apt.doctor.firstName + ' ' + apt.doctor.lastName) : 'Doctor N/A' %>
                                    </div>
                                    <div style="font-size: 0.875rem; color: #64748B;">
                                        <i class="fas fa-calendar"></i> <%= apt.appointmentDate ? new Date(apt.appointmentDate).toLocaleDateString() : 'N/A' %>
                                        <i class="fas fa-clock" style="margin-left: 1rem;"></i> <%= apt.appointmentTime || 'N/A' %>
                                        <% if (apt.doctor && apt.doctor.specialization) { %>
                                            <i class="fas fa-stethoscope" style="margin-left: 1rem;"></i> <%= apt.doctor.specialization %>
                                        <% } %>
                                    </div>
                                </div>
                                <span class="badge badge-<%= apt.status %>">
                                    <%= apt.status.charAt(0).toUpperCase() + apt.status.slice(1) %>
                                </span>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state">
                        <i class="fas fa-calendar-times"></i>
                        <p>No appointments found</p>
                    </div>
                <% } %>
            </div>

            <!-- Prescriptions -->
            <div class="section-card">
                <div class="section-header">
                    <h2><i class="fas fa-pills" style="color: #F59E0B;"></i> Prescriptions</h2>
                    <span style="color: #64748B; font-size: 0.9375rem;"><%= prescriptions.length %> prescriptions</span>
                </div>
                
                <% if (prescriptions && prescriptions.length > 0) { %>
                    <% prescriptions.forEach(rx => { %>
                        <div class="prescription-item">
                            <div style="font-weight: 600; color: #1E293B; margin-bottom: 0.5rem;">
                                Prescribed by Dr. <%= rx.doctor ? (rx.doctor.firstName + ' ' + rx.doctor.lastName) : 'N/A' %>
                            </div>
                            <div style="font-size: 0.875rem; color: #64748B; margin-bottom: 0.5rem;">
                                <i class="fas fa-calendar"></i> <%= rx.prescriptionDate ? new Date(rx.prescriptionDate).toLocaleDateString() : 'N/A' %>
                            </div>
                            <% if (rx.medications && rx.medications.length > 0) { %>
                                <div style="margin-top: 0.75rem;">
                                    <% rx.medications.forEach(med => { %>
                                        <div style="background: white; padding: 0.75rem; border-radius: 0.5rem; margin-bottom: 0.5rem; border-left: 3px solid #F59E0B;">
                                            <strong><%= med.name %></strong> - <%= med.dosage %> 
                                            <span style="color: #64748B;">(<%=med.frequency %>)</span>
                                        </div>
                                    <% }); %>
                                </div>
                            <% } %>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state">
                        <i class="fas fa-prescription"></i>
                        <p>No prescriptions found</p>
                    </div>
                <% } %>
            </div>

            <!-- Test Results -->
            <div class="section-card">
                <div class="section-header">
                    <h2><i class="fas fa-flask" style="color: #14B8A6;"></i> Test Results</h2>
                    <span style="color: #64748B; font-size: 0.9375rem;"><%= testResults.length %> results</span>
                </div>
                
                <% if (testResults && testResults.length > 0) { %>
                    <% testResults.forEach(test => { %>
                        <div class="test-item">
                            <div style="display: flex; justify-content: space-between; align-items: start; flex-wrap: wrap; gap: 1rem;">
                                <div>
                                    <div style="font-weight: 600; color: #1E293B; margin-bottom: 0.25rem;">
                                        <%= test.testName %>
                                    </div>
                                    <div style="font-size: 0.875rem; color: #64748B;">
                                        <i class="fas fa-calendar"></i> <%= test.testDate ? new Date(test.testDate).toLocaleDateString() : 'N/A' %>
                                        <% if (test.result) { %>
                                            <span style="margin-left: 1rem;">Result: <strong><%= test.result %></strong></span>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="empty-state">
                        <i class="fas fa-vial"></i>
                        <p>No test results found</p>
                    </div>
                <% } %>
            </div>

        </main>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        const mobileMenuToggle = document.getElementById('mobileMenuToggle');
        const sidebar = document.querySelector('.sidebar');
        const overlay = document.getElementById('sidebarOverlay');
        
        if (mobileMenuToggle) {
            mobileMenuToggle.addEventListener('click', () => {
                sidebar.classList.toggle('active');
                overlay.classList.toggle('active');
            });
        }
        
        if (overlay) {
            overlay.addEventListener('click', () => {
                sidebar.classList.remove('active');
                overlay.classList.remove('active');
            });
        }
    </script>
</body>
</html>
