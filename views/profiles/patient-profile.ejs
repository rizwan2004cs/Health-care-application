<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile - Bizil Health Care</title>
    <link rel="icon" type="image/png" href="/images/favicon.ico">
    <link rel="shortcut icon" type="image/png" href="/images/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.5.1/css/all.css" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/modern-dashboard.css">
    <style>
        body {
            background: #F8FAFC;
            min-height: 100vh;
            font-family: 'Inter', sans-serif;
            margin: 0;
            overflow-x: hidden;
            overflow-y: auto;
        }
        
        html {
            overflow-y: auto;
            overflow-x: hidden;
        }
        
        .modern-dashboard {
            min-height: 100vh;
            width: 100%;
            height: auto;
        }
        
        .main-content {
            min-height: 100vh;
            height: auto;
            box-sizing: border-box;
            position: relative;
            z-index: 1;
            max-width: 100%;
        }
        
        /* Mobile Menu Toggle Button */
        .mobile-menu-toggle {
            display: none;
            width: 40px;
            height: 40px;
            border-radius: 0.5rem;
            border: 1px solid #E2E8F0;
            background: white;
            cursor: pointer;
            transition: all 0.3s ease;
            align-items: center;
            justify-content: center;
            pointer-events: auto;
            z-index: 1002;
            position: relative;
        }
        
        .mobile-menu-toggle:hover {
            background: #F8FAFC;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }
        
        .mobile-menu-toggle i {
            font-size: 1.25rem;
            color: #1E293B;
        }
        
        /* Mobile Sidebar Overlay */
        .sidebar-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 999;
            opacity: 0;
            transition: opacity 0.3s ease;
            pointer-events: none;
        }
        
        .sidebar-overlay.active {
            opacity: 1;
            pointer-events: auto;
        }
        
        /* Sidebar Mobile States */
        .sidebar.mobile-hidden {
            transform: translateX(-100%);
        }
        
        .sidebar.mobile-visible {
            transform: translateX(0);
        }
        
        /* Profile Form Styles */
        .profile-form-card {
            background: white;
            border-radius: 1rem;
            padding: 2rem;
            margin-bottom: 2rem;
            border: 1px solid #E2E8F0;
            box-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05);
        }
        
        .section-title {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-size: 1.25rem;
            font-weight: 600;
            color: #1E293B;
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid #E2E8F0;
        }
        
        .section-title i {
            color: #8B5CF6;
            font-size: 1.5rem;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-group label {
            display: block;
            color: #64748B;
            font-size: 0.875rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #E2E8F0;
            border-radius: 0.5rem;
            font-size: 0.875rem;
            transition: all 0.2s;
            font-family: 'Inter', sans-serif;
        }
        
        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #8B5CF6;
            box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
        }
        
        .form-group textarea {
            resize: vertical;
            min-height: 100px;
        }
        
        .form-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
        }
        
        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.75rem;
            background: #F8FAFC;
            border-radius: 0.5rem;
            margin-bottom: 1rem;
        }
        
        .checkbox-group input[type="checkbox"] {
            width: 1.25rem;
            height: 1.25rem;
            cursor: pointer;
        }
        
        .checkbox-group label {
            margin: 0;
            color: #1E293B;
            font-weight: 500;
            cursor: pointer;
            text-transform: none;
        }
        
        .btn-submit {
            width: 100%;
            padding: 1rem;
            background: linear-gradient(135deg, #8B5CF6, #6D28D9);
            color: white;
            border: none;
            border-radius: 0.75rem;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 1rem;
        }
        
        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(139, 92, 246, 0.4);
        }
        
        .help-text {
            font-size: 0.75rem;
            color: #64748B;
            margin-top: 0.25rem;
        }
        
        .alert {
            padding: 1rem 1.5rem;
            border-radius: 0.75rem;
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
        
        .alert-success {
            background: rgba(16, 185, 129, 0.1);
            color: #10B981;
            border: 1px solid rgba(16, 185, 129, 0.2);
        }
        
        .alert-danger {
            background: rgba(239, 68, 68, 0.1);
            color: #EF4444;
            border: 1px solid rgba(239, 68, 68, 0.2);
        }
        
        .alert i {
            font-size: 1.25rem;
        }
        
        @media (max-width: 1200px) {
            .modern-dashboard {
                grid-template-columns: 1fr;
            }
            
            .mobile-menu-toggle {
                display: flex;
            }
            
            .sidebar-overlay {
                display: none;
            }
            
            .sidebar-overlay.active {
                display: block;
            }
            
            .sidebar {
                position: fixed;
                left: 0;
                top: 0;
                width: 280px;
                height: 100vh;
                z-index: 1000;
                transform: translateX(-100%);
                transition: transform 0.3s ease;
                box-shadow: 2px 0 8px rgba(0, 0, 0, 0.1);
            }
            
            .sidebar.mobile-visible {
                transform: translateX(0);
            }
            
            .main-content {
                margin-left: 0;
                width: 100%;
            }
        }
        
        @media (max-width: 768px) {
            .form-row {
                grid-template-columns: 1fr;
            }
            
            .profile-form-card {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar Overlay for Mobile -->
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    
    <!-- Modern Dashboard Layout -->
    <div class="modern-dashboard">
        <!-- Sidebar Navigation -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="sidebar-logo">
                    <i class="fas fa-heartbeat"></i>
                </div>
                <div>
                    <h2 class="sidebar-title">Bizil Health</h2>
                </div>
            </div>
            
            <nav>
                <ul class="nav-menu">
                    <li class="nav-item">
                        <a href="/patient/dashboard" class="nav-link">
                            <i class="fas fa-home nav-icon"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/appointments" class="nav-link">
                            <i class="fas fa-calendar-alt nav-icon"></i>
                            <span>Appointments</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/search-doctors" class="nav-link">
                            <i class="fas fa-user-md nav-icon"></i>
                            <span>Find Doctors</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/test-results" class="nav-link">
                            <i class="fas fa-flask nav-icon"></i>
                            <span>Test Results</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/prescriptions" class="nav-link">
                            <i class="fas fa-pills nav-icon"></i>
                            <span>Prescriptions</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/health-tips" class="nav-link">
                            <i class="fas fa-lightbulb nav-icon"></i>
                            <span>Health Tips</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/complete-profile" class="nav-link active">
                            <i class="fas fa-user-edit nav-icon"></i>
                            <span>My Profile</span>
                        </a>
                    </li>
                    <li class="nav-item" style="margin-top: 2rem;">
                        <a href="/patient/logout" class="nav-link" style="color: #EF4444;">
                            <i class="fas fa-sign-out-alt nav-icon"></i>
                            <span>Logout</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>
        
        <!-- Main Content -->
        <main class="main-content">
            <!-- Top Header -->
            <header class="dashboard-header">
                <div class="welcome-section">
                    <div style="display: flex; align-items: center; gap: 1rem;">
                        <button class="mobile-menu-toggle" id="mobileMenuToggle" aria-label="Toggle Menu">
                            <i class="fas fa-bars"></i>
                        </button>
                        <div>
                            <h1 style="margin-bottom: 0.25rem;"><i class="fas fa-user-edit"></i> My Profile</h1>
                            <p style="margin: 0;">Complete your health information for personalized care</p>
                        </div>
                    </div>
                </div>
                
                <div class="header-actions">
                    <div class="user-profile">
                        <div class="user-avatar">
                            <%= (user && user.username) ? user.username.charAt(0).toUpperCase() : 'P' %>
                        </div>
                        <div class="user-info">
                            <div class="user-name"><%= (user && user.username) ? user.username : 'Patient' %></div>
                            <div class="user-role">Patient</div>
                        </div>
                    </div>
                </div>
            </header>
            
            <!-- Flash Messages -->
            <% if (typeof successMessage !== 'undefined' && successMessage) { %>
                <div class="alert alert-success">
                    <i class="fas fa-check-circle"></i>
                    <span><%= successMessage %></span>
                </div>
            <% } %>
            
            <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
                <div class="alert alert-danger">
                    <i class="fas fa-exclamation-circle"></i>
                    <span><%= errorMessage %></span>
                </div>
            <% } %>
            
            <!-- Profile Form -->
            <form method="POST" action="/patient/complete-profile">
                <!-- Emergency Contact Section -->
                <div class="profile-form-card">
                    <h2 class="section-title">
                        <i class="fas fa-user-friends"></i>
                        Emergency Contact
                    </h2>
                    
                    <div class="form-row">
                        <div class="form-group">
                            <label for="emergencyContactName">Contact Name *</label>
                            <input type="text" 
                                   id="emergencyContactName" 
                                   name="emergencyContact[name]" 
                                   value="<%= patient && patient.healthProfile && patient.healthProfile.emergencyContact ? patient.healthProfile.emergencyContact.name : '' %>"
                                   placeholder="Enter emergency contact name"
                                   required>
                        </div>
                        
                        <div class="form-group">
                            <label for="emergencyContactRelationship">Relationship *</label>
                            <input type="text" 
                                   id="emergencyContactRelationship" 
                                   name="emergencyContact[relationship]" 
                                   value="<%= patient && patient.healthProfile && patient.healthProfile.emergencyContact ? patient.healthProfile.emergencyContact.relationship : '' %>"
                                   placeholder="e.g., Spouse, Parent, Sibling"
                                   required>
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label for="emergencyContactPhone">Contact Phone *</label>
                        <input type="tel" 
                               id="emergencyContactPhone" 
                               name="emergencyContact[phone]" 
                               value="<%= patient && patient.healthProfile && patient.healthProfile.emergencyContact ? patient.healthProfile.emergencyContact.phone : '' %>"
                               placeholder="10-digit phone number"
                               pattern="[0-9]{10}"
                               maxlength="10"
                               required>
                        <div class="help-text">Enter 10-digit phone number without spaces or dashes</div>
                    </div>
                </div>
                
                <!-- Health Information Section -->
                <div class="profile-form-card">
                    <h2 class="section-title">
                        <i class="fas fa-heartbeat"></i>
                        Health Information
                    </h2>
                    
                    <div class="form-row">
                        <div class="form-group">
                            <label for="bloodType">Blood Type</label>
                            <select id="bloodType" name="healthProfile[bloodType]">
                                <option value="" <%= !patient || !patient.healthProfile || !patient.healthProfile.bloodType ? 'selected' : '' %>>Select Blood Type</option>
                                <option value="A+" <%= patient && patient.healthProfile && patient.healthProfile.bloodType === 'A+' ? 'selected' : '' %>>A+</option>
                                <option value="A-" <%= patient && patient.healthProfile && patient.healthProfile.bloodType === 'A-' ? 'selected' : '' %>>A-</option>
                                <option value="B+" <%= patient && patient.healthProfile && patient.healthProfile.bloodType === 'B+' ? 'selected' : '' %>>B+</option>
                                <option value="B-" <%= patient && patient.healthProfile && patient.healthProfile.bloodType === 'B-' ? 'selected' : '' %>>B-</option>
                                <option value="AB+" <%= patient && patient.healthProfile && patient.healthProfile.bloodType === 'AB+' ? 'selected' : '' %>>AB+</option>
                                <option value="AB-" <%= patient && patient.healthProfile && patient.healthProfile.bloodType === 'AB-' ? 'selected' : '' %>>AB-</option>
                                <option value="O+" <%= patient && patient.healthProfile && patient.healthProfile.bloodType === 'O+' ? 'selected' : '' %>>O+</option>
                                <option value="O-" <%= patient && patient.healthProfile && patient.healthProfile.bloodType === 'O-' ? 'selected' : '' %>>O-</option>
                            </select>
                        </div>
                        
                        <div class="form-group">
                            <label for="height">Height (cm)</label>
                            <input type="number" 
                                   id="height" 
                                   name="healthProfile[height][value]" 
                                   value="<%= patient && patient.healthProfile && patient.healthProfile.height ? patient.healthProfile.height.value : '' %>"
                                   placeholder="Enter height in centimeters"
                                   min="50"
                                   max="300">
                        </div>
                        
                        <div class="form-group">
                            <label for="weight">Weight (kg)</label>
                            <input type="number" 
                                   id="weight" 
                                   name="healthProfile[weight][value]" 
                                   value="<%= patient && patient.healthProfile && patient.healthProfile.weight ? patient.healthProfile.weight.value : '' %>"
                                   placeholder="Enter weight in kilograms"
                                   min="20"
                                   max="300"
                                   step="0.1">
                        </div>
                    </div>
                </div>
                
                <!-- Body Vitals Section -->
                <div class="profile-form-card">
                    <h2 class="section-title">
                        <i class="fas fa-heart-pulse"></i>
                        Body Vitals (Optional)
                    </h2>
                    
                    <div class="form-row">
                        <div class="form-group">
                            <label for="heartRate">Heart Rate (bpm)</label>
                            <input type="number" 
                                   id="heartRate" 
                                   name="healthProfile[vitalSigns][heartRate]" 
                                   value="<%= patient && patient.healthProfile && patient.healthProfile.vitalSigns ? patient.healthProfile.vitalSigns.heartRate : '' %>"
                                   placeholder="e.g., 72"
                                   min="40"
                                   max="200">
                            <div class="help-text">Normal range: 60-100 bpm</div>
                        </div>
                        
                        <div class="form-group">
                            <label for="oxygenSaturation">Oxygen Saturation (%)</label>
                            <input type="number" 
                                   id="oxygenSaturation" 
                                   name="healthProfile[vitalSigns][oxygenSaturation]" 
                                   value="<%= patient && patient.healthProfile && patient.healthProfile.vitalSigns ? patient.healthProfile.vitalSigns.oxygenSaturation : '' %>"
                                   placeholder="e.g., 98"
                                   min="70"
                                   max="100">
                            <div class="help-text">Normal range: 95-100%</div>
                        </div>
                        
                        <div class="form-group">
                            <label for="bpSystolic">Blood Pressure - Systolic</label>
                            <input type="number" 
                                   id="bpSystolic" 
                                   name="healthProfile[vitalSigns][bloodPressure][systolic]" 
                                   value="<%= patient && patient.healthProfile && patient.healthProfile.vitalSigns && patient.healthProfile.vitalSigns.bloodPressure ? patient.healthProfile.vitalSigns.bloodPressure.systolic : '' %>"
                                   placeholder="e.g., 120"
                                   min="70"
                                   max="200">
                            <div class="help-text">Normal: around 120 mmHg</div>
                        </div>
                        
                        <div class="form-group">
                            <label for="bpDiastolic">Blood Pressure - Diastolic</label>
                            <input type="number" 
                                   id="bpDiastolic" 
                                   name="healthProfile[vitalSigns][bloodPressure][diastolic]" 
                                   value="<%= patient && patient.healthProfile && patient.healthProfile.vitalSigns && patient.healthProfile.vitalSigns.bloodPressure ? patient.healthProfile.vitalSigns.bloodPressure.diastolic : '' %>"
                                   placeholder="e.g., 80"
                                   min="40"
                                   max="130">
                            <div class="help-text">Normal: around 80 mmHg</div>
                        </div>
                    </div>
                </div>
                
                <!-- Medical History Section -->
                <div class="profile-form-card">
                    <h2 class="section-title">
                        <i class="fas fa-notes-medical"></i>
                        Medical History (Optional)
                    </h2>
                    
                    <div class="form-group">
                        <label for="allergies">Allergies</label>
                        <textarea id="allergies" 
                                  name="allergiesText" 
                                  placeholder="List any allergies (medications, food, environmental, etc.)"
                        ><%= patient && patient.healthProfile && patient.healthProfile.allergiesNote ? patient.healthProfile.allergiesNote : '' %></textarea>
                        <div class="help-text">Please list any known allergies</div>
                    </div>
                    
                    <div class="form-group">
                        <label for="medicalHistory">Medical History</label>
                        <textarea id="medicalHistory" 
                                  name="medicalHistoryText" 
                                  placeholder="List any chronic conditions, past surgeries, or ongoing medical concerns"
                        ><%= patient && patient.healthProfile && patient.healthProfile.medicalHistoryNote ? patient.healthProfile.medicalHistoryNote : '' %></textarea>
                        <div class="help-text">Include chronic conditions, past surgeries, and ongoing treatments</div>
                    </div>
                </div>
                
                <!-- Preferences Section -->
                <div class="profile-form-card">
                    <h2 class="section-title">
                        <i class="fas fa-cog"></i>
                        Notification Preferences
                    </h2>
                    
                    <div class="checkbox-group">
                        <input type="checkbox" 
                               id="appointmentReminders" 
                               name="preferences[appointmentReminders]"
                               <%= patient && patient.preferences && patient.preferences.appointmentReminders ? 'checked' : '' %>>
                        <label for="appointmentReminders">
                            <i class="fas fa-calendar-check" style="color: #8B5CF6; margin-right: 0.5rem;"></i>
                            Receive appointment reminders via email/SMS
                        </label>
                    </div>
                    
                    <div class="checkbox-group">
                        <input type="checkbox" 
                               id="healthTips" 
                               name="preferences[healthTips]"
                               <%= patient && patient.preferences && patient.preferences.healthTips ? 'checked' : '' %>>
                        <label for="healthTips">
                            <i class="fas fa-lightbulb" style="color: #F59E0B; margin-right: 0.5rem;"></i>
                            Receive health tips and wellness information
                        </label>
                    </div>
                </div>
                
                <!-- Submit Button -->
                <button type="submit" class="btn-submit">
                    <i class="fas fa-save"></i> Save Profile Information
                </button>
            </form>
        </main>
    </div>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Mobile Menu Toggle Functionality
        const mobileMenuToggle = document.getElementById('mobileMenuToggle');
        const sidebar = document.querySelector('.sidebar');
        const sidebarOverlay = document.getElementById('sidebarOverlay');
        const navLinks = document.querySelectorAll('.nav-link');
        
        function toggleMenu() {
            sidebar.classList.toggle('mobile-visible');
            sidebarOverlay.classList.toggle('active');
            
            const icon = mobileMenuToggle.querySelector('i');
            if (sidebar.classList.contains('mobile-visible')) {
                icon.classList.remove('fa-bars');
                icon.classList.add('fa-times');
            } else {
                icon.classList.remove('fa-times');
                icon.classList.add('fa-bars');
            }
        }
        
        function closeMenu() {
            sidebar.classList.remove('mobile-visible');
            sidebarOverlay.classList.remove('active');
            const icon = mobileMenuToggle.querySelector('i');
            icon.classList.remove('fa-times');
            icon.classList.add('fa-bars');
        }
        
        if (mobileMenuToggle) {
            mobileMenuToggle.addEventListener('click', toggleMenu);
        }
        
        if (sidebarOverlay) {
            sidebarOverlay.addEventListener('click', closeMenu);
        }
        
        navLinks.forEach(link => {
            link.addEventListener('click', () => {
                if (window.innerWidth <= 1200) {
                    closeMenu();
                }
            });
        });
        
        window.addEventListener('resize', () => {
            if (window.innerWidth > 1200) {
                closeMenu();
            }
        });
        
        console.log('Profile page loaded successfully');
    </script>
</body>
</html>
