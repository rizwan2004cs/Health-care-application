<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Health Tips - Bizil Health Care</title>
    <link rel="icon" type="image/png" href="/images/favicon.ico">
    <link rel="shortcut icon" type="image/png" href="/images/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/modern-dashboard.css">
    <style>
        body {
            background: #F8FAFC;
            min-height: 100vh;
            font-family: 'Inter', sans-serif;
        }
        
        .search-section {
            background: white;
            border-radius: 16px;
            padding: 2rem;
            margin-bottom: 2rem;
            border: 1px solid #E5E7EB;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
        }
        
        .search-form {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            align-items: end;
        }
        
        .search-group {
            display: flex;
            flex-direction: column;
        }
        
        .search-group label {
            font-weight: 600;
            color: #374151;
            margin-bottom: 0.5rem;
            font-size: 0.9375rem;
        }
        
        .search-group input,
        .search-group select {
            padding: 0.875rem 1rem;
            border: 1px solid #D1D5DB;
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: white;
            color: #111827;
        }
        
        .search-group input:focus,
        .search-group select:focus {
            outline: none;
            border-color: #8B5CF6;
            box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
        }
        
        .search-group input::placeholder {
            color: #9CA3AF;
        }
        
        .search-btn {
            background: #8B5CF6;
            color: white;
            padding: 0.875rem 2rem;
            border-radius: 10px;
            border: none;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }
        
        .search-btn:hover {
            background: #7C3AED;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
        }
        
        .section-title {
            font-size: 1.25rem;
            font-weight: 700;
            color: #111827;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
        
        .section-title i {
            color: #8B5CF6;
        }
        
        .tips-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .tip-card {
            background: white;
            border-radius: 16px;
            padding: 1.75rem;
            border: 1px solid #E5E7EB;
            transition: all 0.3s ease;
            position: relative;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
        }
        
        .tip-card:hover {
            border-color: #8B5CF6;
            box-shadow: 0 8px 24px rgba(139, 92, 246, 0.15);
            transform: translateY(-4px);
        }
        
        .tip-card.featured {
            border: 2px solid #F59E0B;
            background: linear-gradient(135deg, #FFF7ED 0%, white 100%);
        }
        
        .featured-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: linear-gradient(135deg, #fbbf24, #f59e0b);
            color: white;
            padding: 0.375rem 0.875rem;
            border-radius: 50px;
            font-size: 0.75rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.375rem;
        }
        
        .tip-category {
            background: #F3F0FF;
            color: #8B5CF6;
            padding: 0.375rem 0.875rem;
            border-radius: 50px;
            font-size: 0.8125rem;
            font-weight: 600;
            display: inline-block;
            margin-bottom: 1rem;
        }
        
        .tip-title {
            color: #111827;
            font-size: 1.125rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
            line-height: 1.5;
        }
        
        .tip-summary {
            color: #6B7280;
            font-size: 0.9375rem;
            line-height: 1.6;
            margin-bottom: 1.25rem;
        }
        
        .tip-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
            font-size: 0.875rem;
            color: #6B7280;
        }
        
        .tip-meta span {
            display: flex;
            align-items: center;
            gap: 0.375rem;
        }
        
        .tip-actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 1rem;
            border-top: 1px solid #E5E7EB;
        }
        
        .tip-engagement {
            display: flex;
            gap: 1rem;
            align-items: center;
        }
        
        .engagement-btn {
            background: none;
            border: none;
            color: #6B7280;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.375rem;
            padding: 0.375rem 0.75rem;
            border-radius: 8px;
            font-size: 0.875rem;
            font-weight: 500;
        }
        
        .engagement-btn:hover {
            color: #8B5CF6;
            background: #F3F0FF;
        }
        
        .engagement-btn.liked {
            color: #EF4444;
        }
        
        .engagement-btn.bookmarked {
            color: #F59E0B;
        }
        
        .read-more-btn {
            background: #8B5CF6;
            border: none;
            color: white;
            padding: 0.625rem 1.25rem;
            border-radius: 8px;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.3s ease;
            font-size: 0.875rem;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .read-more-btn:hover {
            color: white;
            background: #7C3AED;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
        }
        
        .no-tips {
            text-align: center;
            padding: 4rem 2rem;
            color: #6B7280;
            background: white;
            border-radius: 16px;
            border: 2px dashed #E5E7EB;
        }
        
        .no-tips i {
            font-size: 4rem;
            color: #D1D5DB;
            margin-bottom: 1rem;
        }
        
        .no-tips h3 {
            color: #111827;
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        @media (max-width: 768px) {
            .search-form {
                grid-template-columns: 1fr;
            }
            
            .tips-grid {
                grid-template-columns: 1fr;
            }
            
            .tip-actions {
                flex-direction: column;
                gap: 1rem;
                align-items: flex-start;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar Overlay for Mobile -->
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    
    <!-- Modern Dashboard Layout -->
    <div class="dashboard">
        <!-- Sidebar Navigation -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="sidebar-logo">
                    <i class="fas fa-heartbeat"></i>
                </div>
                <div>
                    <h2 class="sidebar-title">Bizil Health</h2>
                </div>
            </div>
            
            <nav>
                <ul class="nav-menu">
                    <li class="nav-item">
                        <a href="/patient/dashboard" class="nav-link">
                            <i class="fas fa-home nav-icon"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/appointments" class="nav-link">
                            <i class="fas fa-calendar-alt nav-icon"></i>
                            <span>Appointments</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/search-doctors" class="nav-link">
                            <i class="fas fa-user-md nav-icon"></i>
                            <span>Find Doctors</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/test-results" class="nav-link">
                            <i class="fas fa-flask nav-icon"></i>
                            <span>Test Results</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/prescriptions" class="nav-link">
                            <i class="fas fa-pills nav-icon"></i>
                            <span>Prescriptions</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/health-tips" class="nav-link active">
                            <i class="fas fa-lightbulb nav-icon"></i>
                            <span>Health Tips</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/complete-profile" class="nav-link">
                            <i class="fas fa-user-edit nav-icon"></i>
                            <span>My Profile</span>
                        </a>
                    </li>
                    <li class="nav-item" style="margin-top: 2rem;">
                        <a href="/patient/logout" class="nav-link" style="color: #EF4444;">
                            <i class="fas fa-sign-out-alt nav-icon"></i>
                            <span>Logout</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>
        
        <!-- Main Content -->
        <main class="main-content">
            <!-- Top Header -->
            <header class="dashboard-header">
                <div class="welcome-section">
                    <div style="display: flex; align-items: center; gap: 1rem;">
                        <button class="mobile-menu-toggle" id="mobileMenuToggle" aria-label="Toggle Menu">
                            <i class="fas fa-bars"></i>
                        </button>
                        <div>
                            <h1 style="margin-bottom: 0.25rem;"><i class="fas fa-lightbulb"></i> Health Tips & Wellness</h1>
                            <p style="margin: 0;">Expert advice and tips for a healthier lifestyle</p>
                        </div>
                    </div>
                </div>
                
                <div class="header-actions">
                    <a href="/patient/health-tips/generate-page" class="btn" style="background: linear-gradient(135deg, #8B5CF6, #3B82F6); color: white; border: none; padding: 0.625rem 1.25rem; border-radius: 10px; font-weight: 600; display: flex; align-items: center; gap: 0.5rem; text-decoration: none; margin-right: 1rem; white-space: nowrap;">
                        <i class="fas fa-magic"></i>
                        <span class="d-none d-md-inline">Generate AI Tips</span>
                        <span class="d-inline d-md-none">AI Tips</span>
                    </a>
                    
                    <div class="user-profile">
                        <div class="user-avatar">
                            <%= (patient && patient.firstName) ? patient.firstName.charAt(0).toUpperCase() : ((user && user.username) ? user.username.charAt(0).toUpperCase() : 'P') %>
                        </div>
                        <div class="user-info">
                            <div class="user-name"><%= (patient && patient.firstName) ? (patient.firstName + ' ' + (patient.lastName || '')) : ((user && user.username) ? user.username : 'Patient') %></div>
                            <div class="user-role">Patient</div>
                        </div>
                    </div>
                </div>
            </header>
            
            <!-- Flash Messages -->
            <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="fas fa-exclamation-circle me-2"></i><%= errorMessage %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>
            
            <% if (typeof successMessage !== 'undefined' && successMessage) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle me-2"></i><%= successMessage %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>
            
            <!-- Search Section -->
            <div class="search-section">
                <form method="GET" action="/patient/health-tips" class="search-form">
                    <div class="search-group">
                        <label for="category">Category</label>
                        <select name="category" id="category">
                            <option value="all" <%= filters.category === 'all' ? 'selected' : '' %>>All Categories</option>
                            <% categories.forEach(cat => { %>
                                <option value="<%= cat %>" <%= filters.category === cat ? 'selected' : '' %>>
                                    <%= cat.replace('-', ' ').replace(/\b\w/g, l => l.toUpperCase()) %>
                                </option>
                            <% }); %>
                        </select>
                    </div>
                    
                    <div class="search-group">
                        <label for="search">Search Tips</label>
                        <input type="text" name="search" id="search" 
                               placeholder="Search health tips..."
                               value="<%= filters.search || '' %>">
                    </div>
                    
                    <div class="search-group" style="align-self: end;">
                        <button type="submit" class="search-btn">
                            <i class="fas fa-search"></i> Search
                        </button>
                    </div>
                </form>
            </div>
            
            <!-- Featured Tips -->
            <% if (featuredTips && featuredTips.length > 0) { %>
                <div style="margin-bottom: 3rem;">
                    <h2 class="section-title">
                        <i class="fas fa-star"></i>
                        Featured Health Tips
                    </h2>
                    
                    <div class="tips-grid">
                        <% featuredTips.forEach(tip => { %>
                            <div class="tip-card featured">
                                <div class="featured-badge">
                                    <i class="fas fa-star"></i> Featured
                                </div>
                                
                                <div class="tip-category">
                                    <%= tip.category.replace('-', ' ').replace(/\b\w/g, l => l.toUpperCase()) %>
                                </div>
                                
                                <h3 class="tip-title"><%= tip.title %></h3>
                                
                                <p class="tip-summary"><%= tip.summary %></p>
                                
                                <div class="tip-meta">
                                    <span><i class="fas fa-clock"></i> <%= tip.estimatedReadTime %> min read</span>
                                    <span><i class="fas fa-eye"></i> <%= tip.engagement.views %> views</span>
                                </div>
                                
                                <div class="tip-actions">
                                    <div class="tip-engagement">
                                        <button class="engagement-btn" onclick="toggleLike('<%= tip._id %>')">
                                            <i class="fas fa-heart"></i>
                                            <span id="likes-<%= tip._id %>"><%= tip.engagement.likes %></span>
                                        </button>
                                        <button class="engagement-btn" onclick="toggleBookmark('<%= tip._id %>')">
                                            <i class="fas fa-bookmark"></i>
                                            <span id="bookmarks-<%= tip._id %>"><%= tip.engagement.bookmarks %></span>
                                        </button>
                                    </div>
                                    
                                    <a href="/patient/health-tips/<%= tip._id %>" class="read-more-btn">
                                        Read More <i class="fas fa-arrow-right"></i>
                                    </a>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            <% } %>
            
            <!-- Personalized Tips -->
            <% if (personalizedTips && personalizedTips.length > 0) { %>
                <div style="margin-bottom: 3rem;">
                    <h2 class="section-title">
                        <i class="fas fa-user"></i>
                        Personalized for You
                    </h2>
                    
                    <div class="tips-grid">
                        <% personalizedTips.forEach(tip => { %>
                            <div class="tip-card">
                                <div class="tip-category">
                                    <%= tip.category.replace('-', ' ').replace(/\b\w/g, l => l.toUpperCase()) %>
                                </div>
                                
                                <h3 class="tip-title"><%= tip.title %></h3>
                                
                                <p class="tip-summary"><%= tip.summary %></p>
                                
                                <div class="tip-meta">
                                    <span><i class="fas fa-clock"></i> <%= tip.estimatedReadTime %> min read</span>
                                    <span><i class="fas fa-eye"></i> <%= tip.engagement.views %> views</span>
                                </div>
                                
                                <div class="tip-actions">
                                    <div class="tip-engagement">
                                        <button class="engagement-btn" onclick="toggleLike('<%= tip._id %>')">
                                            <i class="fas fa-heart"></i>
                                            <span id="likes-<%= tip._id %>"><%= tip.engagement.likes %></span>
                                        </button>
                                        <button class="engagement-btn" onclick="toggleBookmark('<%= tip._id %>')">
                                            <i class="fas fa-bookmark"></i>
                                            <span id="bookmarks-<%= tip._id %>"><%= tip.engagement.bookmarks %></span>
                                        </button>
                                    </div>
                                    
                                    <a href="/patient/health-tips/<%= tip._id %>" class="read-more-btn">
                                        Read More <i class="fas fa-arrow-right"></i>
                                    </a>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            <% } %>
            
            <!-- All Health Tips -->
            <div style="margin-bottom: 3rem;">
                <h2 class="section-title">
                    <i class="fas fa-list"></i>
                    All Health Tips
                </h2>
                
                <% if (healthTips && healthTips.length > 0) { %>
                    <div class="tips-grid">
                        <% healthTips.forEach(tip => { %>
                            <div class="tip-card">
                                <% if (tip.featured) { %>
                                    <div class="featured-badge">
                                        <i class="fas fa-star"></i> Featured
                                    </div>
                                <% } %>
                                
                                <div class="tip-category">
                                    <%= tip.category.replace('-', ' ').replace(/\b\w/g, l => l.toUpperCase()) %>
                                </div>
                                
                                <h3 class="tip-title"><%= tip.title %></h3>
                                
                                <p class="tip-summary"><%= tip.summary %></p>
                                
                                <div class="tip-meta">
                                    <span><i class="fas fa-clock"></i> <%= tip.estimatedReadTime %> min read</span>
                                    <span><i class="fas fa-eye"></i> <%= tip.engagement.views %> views</span>
                                </div>
                                
                                <div class="tip-actions">
                                    <div class="tip-engagement">
                                        <button class="engagement-btn" onclick="toggleLike('<%= tip._id %>')">
                                            <i class="fas fa-heart"></i>
                                            <span id="likes-<%= tip._id %>"><%= tip.engagement.likes %></span>
                                        </button>
                                        <button class="engagement-btn" onclick="toggleBookmark('<%= tip._id %>')">
                                            <i class="fas fa-bookmark"></i>
                                            <span id="bookmarks-<%= tip._id %>"><%= tip.engagement.bookmarks %></span>
                                        </button>
                                    </div>
                                    
                                    <a href="/patient/health-tips/<%= tip._id %>" class="read-more-btn">
                                        Read More <i class="fas fa-arrow-right"></i>
                                    </a>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                <% } else { %>
                    <div class="no-tips">
                        <i class="fas fa-lightbulb"></i>
                        <h3>No health tips found</h3>
                        <p>Try adjusting your search criteria or browse all available tips.</p>
                        <a href="/patient/health-tips" class="read-more-btn" style="display: inline-flex; margin-top: 1rem;">
                            View All Tips
                        </a>
                    </div>
                <% } %>
            </div>
        </main>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Mobile menu toggle
        const menuToggle = document.getElementById('mobileMenuToggle');
        const sidebar = document.querySelector('.sidebar');
        const overlay = document.getElementById('sidebarOverlay');
        
        if (menuToggle) {
            menuToggle.addEventListener('click', function() {
                sidebar.classList.add('active');
                if (overlay) overlay.classList.add('active');
            });
        }
        
        if (overlay) {
            overlay.addEventListener('click', function() {
                sidebar.classList.remove('active');
                overlay.classList.remove('active');
            });
        }
        
        // Close mobile menu on window resize
        window.addEventListener('resize', function() {
            if (window.innerWidth > 1200) {
                sidebar.classList.remove('active');
                if (overlay) overlay.classList.remove('active');
            }
        });
        
        async function toggleLike(tipId) {
            try {
                const response = await fetch(`/api/patient/health-tip/like/${tipId}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });
                
                const data = await response.json();
                
                if (data.success) {
                    const likesSpan = document.getElementById(`likes-${tipId}`);
                    const likeBtn = likesSpan.parentElement;
                    
                    likesSpan.textContent = data.likesCount;
                    
                    if (data.liked) {
                        likeBtn.classList.add('liked');
                    } else {
                        likeBtn.classList.remove('liked');
                    }
                }
            } catch (error) {
                console.error('Error toggling like:', error);
            }
        }
        
        async function toggleBookmark(tipId) {
            try {
                const response = await fetch(`/api/patient/health-tip/bookmark/${tipId}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                });
                
                const data = await response.json();
                
                if (data.success) {
                    const bookmarksSpan = document.getElementById(`bookmarks-${tipId}`);
                    const bookmarkBtn = bookmarksSpan.parentElement;
                    
                    bookmarksSpan.textContent = data.bookmarksCount;
                    
                    if (data.bookmarked) {
                        bookmarkBtn.classList.add('bookmarked');
                    } else {
                        bookmarkBtn.classList.remove('bookmarked');
                    }
                }
            } catch (error) {
                console.error('Error toggling bookmark:', error);
            }
        }
    </script>
</body>
</html>
