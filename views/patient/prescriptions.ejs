<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Prescriptions - Bizil Health Care</title>
    <link rel="icon" type="image/png" href="/images/favicon.ico">
    <link rel="shortcut icon" type="image/png" href="/images/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.5.1/css/all.css" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/modern-dashboard.css">
    <style>
        body {
            background: #F8FAFC;
            min-height: 100vh;
            font-family: 'Inter', sans-serif;
            margin: 0;
            overflow-x: hidden;
            overflow-y: auto;
        }
        
        html {
            overflow-y: auto;
            overflow-x: hidden;
        }
        
        .modern-dashboard {
            min-height: 100vh;
            width: 100%;
            height: auto;
        }
        
        .main-content {
            min-height: 100vh;
            height: auto;
            box-sizing: border-box;
            position: relative;
            z-index: 1;
            max-width: 100%;
        }
        
        /* Mobile Menu Toggle Button */
        .mobile-menu-toggle {
            display: none;
            width: 40px;
            height: 40px;
            border-radius: 0.5rem;
            border: 1px solid #E2E8F0;
            background: white;
            cursor: pointer;
            transition: all 0.3s ease;
            align-items: center;
            justify-content: center;
            pointer-events: auto;
            z-index: 1002;
            position: relative;
        }
        
        .mobile-menu-toggle:hover {
            background: #F8FAFC;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }
        
        .mobile-menu-toggle i {
            font-size: 1.25rem;
            color: #1E293B;
        }
        
        /* Mobile Sidebar Overlay */
        .sidebar-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.5);
            z-index: 999;
            opacity: 0;
            transition: opacity 0.3s ease;
            pointer-events: none;
        }
        
        .sidebar-overlay.active {
            opacity: 1;
            pointer-events: auto;
        }
        
        /* Sidebar Mobile States */
        .sidebar.mobile-hidden {
            transform: translateX(-100%);
        }
        
        .sidebar.mobile-visible {
            transform: translateX(0);
        }
        
        /* Ensure all interactive elements are clickable */
        a, button, input, select, textarea, .nav-link {
            pointer-events: auto !important;
            cursor: pointer;
        }
        
        /* Prescription specific styles */
        .prescription-card {
            background: white;
            border-radius: 1rem;
            padding: 1.5rem;
            margin-bottom: 1rem;
            border: 1px solid #E2E8F0;
            box-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            transition: all 0.3s ease;
        }
        
        .prescription-card:hover {
            box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
            transform: translateY(-2px);
        }
        
        .prescription-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
        }
        
        .medication-name {
            font-size: 1.25rem;
            font-weight: 600;
            color: #1E293B;
            margin-bottom: 0.25rem;
        }
        
        .medication-strength {
            color: #8B5CF6;
            font-weight: 500;
            font-size: 0.875rem;
        }
        
        .prescription-badge {
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.75rem;
            font-weight: 600;
        }
        
        .prescription-badge.active {
            background: rgba(16, 185, 129, 0.1);
            color: #10B981;
        }
        
        .prescription-badge.expired {
            background: rgba(239, 68, 68, 0.1);
            color: #EF4444;
        }
        
        .prescription-badge.needs-refill {
            background: rgba(245, 158, 11, 0.1);
            color: #F59E0B;
        }
        
        .medication-details-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1rem;
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid #E2E8F0;
        }
        
        .detail-item {
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
        }
        
        .detail-label {
            color: #64748B;
            font-size: 0.75rem;
            text-transform: uppercase;
            font-weight: 600;
            letter-spacing: 0.5px;
        }
        
        .detail-value {
            color: #1E293B;
            font-weight: 600;
            font-size: 0.875rem;
        }
        
        .refill-progress {
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px solid #E2E8F0;
        }
        
        .refill-progress-label {
            display: flex;
            justify-content: space-between;
            color: #64748B;
            font-size: 0.875rem;
            margin-bottom: 0.5rem;
        }
        
        .progress-bar-container {
            background: #F1F5F9;
            border-radius: 10px;
            height: 8px;
            overflow: hidden;
        }
        
        .progress-bar {
            height: 100%;
            border-radius: 10px;
            transition: width 0.3s ease;
            background: linear-gradient(90deg, #10B981, #059669);
        }
        
        .progress-bar.warning {
            background: linear-gradient(90deg, #F59E0B, #D97706);
        }
        
        .progress-bar.danger {
            background: linear-gradient(90deg, #EF4444, #DC2626);
        }
        
        .no-prescriptions {
            text-align: center;
            padding: 4rem 2rem;
            background: white;
            border-radius: 1.5rem;
            border: 2px dashed #E2E8F0;
        }
        
        .no-prescriptions i {
            font-size: 4rem;
            color: #CBD5E1;
            margin-bottom: 1rem;
        }
        
        .no-prescriptions h3 {
            color: #1E293B;
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }
        
        .no-prescriptions p {
            color: #64748B;
            margin: 0;
        }
        
        @media (max-width: 1200px) {
            .modern-dashboard {
                grid-template-columns: 1fr;
            }
            
            .mobile-menu-toggle {
                display: flex;
            }
            
            .sidebar-overlay {
                display: none;
            }
            
            .sidebar-overlay.active {
                display: block;
            }
            
            .sidebar {
                position: fixed;
                left: 0;
                top: 0;
                width: 280px;
                height: 100vh;
                z-index: 1000;
                transform: translateX(-100%);
                transition: transform 0.3s ease;
                box-shadow: 2px 0 8px rgba(0, 0, 0, 0.1);
            }
            
            .sidebar.mobile-visible {
                transform: translateX(0);
            }
            
            .main-content {
                margin-left: 0;
                width: 100%;
            }
        }
        
        @media (max-width: 768px) {
            .medication-details-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar Overlay for Mobile -->
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    
    <!-- Modern Dashboard Layout -->
    <div class="modern-dashboard">
        <!-- Sidebar Navigation -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="sidebar-logo">
                    <i class="fas fa-heartbeat"></i>
                </div>
                <div>
                    <h2 class="sidebar-title">Bizil Health</h2>
                </div>
            </div>
            
            <nav>
                <ul class="nav-menu">
                    <li class="nav-item">
                        <a href="/patient/dashboard" class="nav-link">
                            <i class="fas fa-home nav-icon"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/appointments" class="nav-link">
                            <i class="fas fa-calendar-alt nav-icon"></i>
                            <span>Appointments</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/search-doctors" class="nav-link">
                            <i class="fas fa-user-md nav-icon"></i>
                            <span>Find Doctors</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/test-results" class="nav-link">
                            <i class="fas fa-flask nav-icon"></i>
                            <span>Test Results</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/prescriptions" class="nav-link active">
                            <i class="fas fa-pills nav-icon"></i>
                            <span>Prescriptions</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/health-tips" class="nav-link">
                            <i class="fas fa-lightbulb nav-icon"></i>
                            <span>Health Tips</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/complete-profile" class="nav-link">
                            <i class="fas fa-user-edit nav-icon"></i>
                            <span>My Profile</span>
                        </a>
                    </li>
                    <li class="nav-item" style="margin-top: 2rem;">
                        <a href="/patient/logout" class="nav-link" style="color: #EF4444;">
                            <i class="fas fa-sign-out-alt nav-icon"></i>
                            <span>Logout</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>
        
        <!-- Main Content -->
        <main class="main-content">
            <!-- Top Header -->
            <header class="dashboard-header">
                <div class="welcome-section">
                    <div style="display: flex; align-items: center; gap: 1rem;">
                        <button class="mobile-menu-toggle" id="mobileMenuToggle" aria-label="Toggle Menu">
                            <i class="fas fa-bars"></i>
                        </button>
                        <div>
                            <h1 style="margin-bottom: 0.25rem;"><i class="fas fa-pills"></i> My Prescriptions</h1>
                            <p style="margin: 0;">Manage your medications and track refills</p>
                        </div>
                    </div>
                </div>
                
                <div class="header-actions">
                    <div class="user-profile">
                        <div class="user-avatar">
                            <%= (user && user.username) ? user.username.charAt(0).toUpperCase() : 'P' %>
                        </div>
                        <div class="user-info">
                            <div class="user-name"><%= (user && user.username) ? user.username : 'Patient' %></div>
                            <div class="user-role">Patient</div>
                        </div>
                    </div>
                </div>
            </header>
            
            <!-- Stats Cards -->
            <div class="stats-grid" style="margin-bottom: 2rem;">
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon purple">
                            <i class="fas fa-pills"></i>
                        </div>
                    </div>
                    <div class="stat-value"><%= prescriptions ? prescriptions.length : 0 %></div>
                    <div class="stat-label">Total Prescriptions</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon blue">
                            <i class="fas fa-check-circle"></i>
                        </div>
                    </div>
                    <div class="stat-value"><%= prescriptions ? prescriptions.filter(p => p.status === 'active').length : 0 %></div>
                    <div class="stat-label">Active</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon pink">
                            <i class="fas fa-exclamation-triangle"></i>
                        </div>
                    </div>
                    <div class="stat-value"><%= prescriptions ? prescriptions.filter(p => p.refillsRemaining && p.refillsRemaining <= 1).length : 0 %></div>
                    <div class="stat-label">Need Refill</div>
                </div>
                
                <div class="stat-card">
                    <div class="stat-header">
                        <div class="stat-icon teal">
                            <i class="fas fa-clock"></i>
                        </div>
                    </div>
                    <div class="stat-value"><%= prescriptions ? prescriptions.filter(p => p.status === 'expired').length : 0 %></div>
                    <div class="stat-label">Expired</div>
                </div>
            </div>
            
            <!-- Prescriptions List -->
            <% if (prescriptions && prescriptions.length > 0) { %>
                <% prescriptions.forEach(function(prescription) { 
                    const refillPercent = prescription.refillsRemaining ? (prescription.refillsRemaining / (prescription.refillsRemaining + (prescription.refillsUsed || 0))) * 100 : 0;
                    let progressClass = '';
                    if (refillPercent <= 20) progressClass = 'danger';
                    else if (refillPercent <= 50) progressClass = 'warning';
                %>
                    <div class="prescription-card">
                        <div class="prescription-header">
                            <div>
                                <div class="medication-name">
                                    Rx #<%= prescription.prescriptionNumber || 'N/A' %>
                                </div>
                                <div class="medication-strength">
                                    <%= prescription.diagnosis && prescription.diagnosis.primary ? prescription.diagnosis.primary : 'General Prescription' %>
                                </div>
                            </div>
                            <span class="prescription-badge <%= prescription.status || 'active' %>">
                                <%= prescription.status ? prescription.status.toUpperCase() : 'ACTIVE' %>
                            </span>
                        </div>
                        
                        <div class="medication-details-grid">
                            <div class="detail-item">
                                <div class="detail-label">Doctor</div>
                                <div class="detail-value">
                                    <%= prescription.doctor ? ('Dr. ' + prescription.doctor.firstName + ' ' + prescription.doctor.lastName) : 'N/A' %>
                                </div>
                            </div>
                            
                            <div class="detail-item">
                                <div class="detail-label">Prescription Date</div>
                                <div class="detail-value">
                                    <%= prescription.prescriptionDate ? new Date(prescription.prescriptionDate).toLocaleDateString() : 'N/A' %>
                                </div>
                            </div>
                            
                            <div class="detail-item">
                                <div class="detail-label">Medications</div>
                                <div class="detail-value"><%= prescription.medications ? prescription.medications.length : 0 %> item(s)</div>
                            </div>
                            
                            <div class="detail-item">
                                <div class="detail-label">Status</div>
                                <div class="detail-value"><%= prescription.status || 'Active' %></div>
                            </div>
                        </div>
                        
                        <!-- Medications List -->
                        <% if (prescription.medications && prescription.medications.length > 0) { %>
                        <div style="margin-top: 1.5rem;">
                            <div style="color: #64748B; font-size: 0.75rem; text-transform: uppercase; font-weight: 600; margin-bottom: 1rem;">
                                Prescribed Medications
                            </div>
                            <% prescription.medications.forEach((med, index) => { %>
                            <div style="background: #F8FAFC; border-radius: 0.5rem; padding: 1rem; margin-bottom: 0.75rem; border-left: 3px solid #8B5CF6;">
                                <div style="font-weight: 600; color: #1E293B; margin-bottom: 0.5rem;">
                                    <%= index + 1 %>. <%= med.name %> - <%= med.strength %>
                                    <span style="color: #64748B; font-weight: 400; font-size: 0.875rem;">
                                        (<%= med.dosageForm %>)
                                    </span>
                                </div>
                                
                                <% if (med.dosage) { %>
                                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 0.75rem; margin-top: 0.5rem;">
                                    <% if (med.dosage.quantity) { %>
                                    <div>
                                        <span style="color: #64748B; font-size: 0.75rem;">Dosage:</span>
                                        <span style="color: #1E293B; font-weight: 500;"> <%= med.dosage.quantity %></span>
                                    </div>
                                    <% } %>
                                    
                                    <% if (med.dosage.frequency) { %>
                                    <div>
                                        <span style="color: #64748B; font-size: 0.75rem;">Frequency:</span>
                                        <span style="color: #1E293B; font-weight: 500;"> <%= med.dosage.frequency %></span>
                                    </div>
                                    <% } %>
                                    
                                    <% if (med.dosage.timing) { %>
                                    <div>
                                        <span style="color: #64748B; font-size: 0.75rem;">Timing:</span>
                                        <span style="color: #1E293B; font-weight: 500;"> <%= med.dosage.timing.replace(/-/g, ' ') %></span>
                                    </div>
                                    <% } %>
                                    
                                    <% if (med.dosage.duration) { %>
                                    <div>
                                        <span style="color: #64748B; font-size: 0.75rem;">Duration:</span>
                                        <span style="color: #1E293B; font-weight: 500;"> <%= med.dosage.duration %></span>
                                    </div>
                                    <% } %>
                                </div>
                                <% } %>
                                
                                <% if (med.instructions) { %>
                                <div style="margin-top: 0.5rem; padding-top: 0.5rem; border-top: 1px solid #E2E8F0;">
                                    <span style="color: #64748B; font-size: 0.75rem;">Instructions: </span>
                                    <span style="color: #475569; font-size: 0.875rem;"><%= med.instructions %></span>
                                </div>
                                <% } %>
                            </div>
                            <% }); %>
                        </div>
                        <% } %>
                        
                        <% if (prescription.instructions && (prescription.instructions.general || prescription.instructions.dietary || prescription.instructions.activity)) { %>
                        <div style="margin-top: 1rem; padding: 1rem; background: #EEF2FF; border-radius: 0.5rem; border-left: 3px solid #8B5CF6;">
                            <div style="color: #6D28D9; font-size: 0.75rem; text-transform: uppercase; font-weight: 600; margin-bottom: 0.5rem;">
                                General Instructions
                            </div>
                            <% if (prescription.instructions.general) { %>
                            <div style="color: #1E293B; font-size: 0.875rem; margin-bottom: 0.5rem;">
                                <%= prescription.instructions.general %>
                            </div>
                            <% } %>
                            <% if (prescription.instructions.dietary) { %>
                            <div style="color: #475569; font-size: 0.875rem; margin-bottom: 0.25rem;">
                                <strong>Dietary:</strong> <%= prescription.instructions.dietary %>
                            </div>
                            <% } %>
                            <% if (prescription.instructions.activity) { %>
                            <div style="color: #475569; font-size: 0.875rem;">
                                <strong>Activity:</strong> <%= prescription.instructions.activity %>
                            </div>
                            <% } %>
                        </div>
                        <% } %>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="no-prescriptions">
                    <i class="fas fa-pills"></i>
                    <h3>No Prescriptions Yet</h3>
                    <p>Your prescriptions will appear here once your doctor prescribes medications.</p>
                </div>
            <% } %>
        </main>
    </div>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Mobile Menu Toggle Functionality
        const mobileMenuToggle = document.getElementById('mobileMenuToggle');
        const sidebar = document.querySelector('.sidebar');
        const sidebarOverlay = document.getElementById('sidebarOverlay');
        const navLinks = document.querySelectorAll('.nav-link');
        
        function toggleMenu() {
            sidebar.classList.toggle('mobile-visible');
            sidebarOverlay.classList.toggle('active');
            
            const icon = mobileMenuToggle.querySelector('i');
            if (sidebar.classList.contains('mobile-visible')) {
                icon.classList.remove('fa-bars');
                icon.classList.add('fa-times');
            } else {
                icon.classList.remove('fa-times');
                icon.classList.add('fa-bars');
            }
        }
        
        function closeMenu() {
            sidebar.classList.remove('mobile-visible');
            sidebarOverlay.classList.remove('active');
            const icon = mobileMenuToggle.querySelector('i');
            icon.classList.remove('fa-times');
            icon.classList.add('fa-bars');
        }
        
        if (mobileMenuToggle) {
            mobileMenuToggle.addEventListener('click', toggleMenu);
        }
        
        if (sidebarOverlay) {
            sidebarOverlay.addEventListener('click', closeMenu);
        }
        
        navLinks.forEach(link => {
            link.addEventListener('click', () => {
                if (window.innerWidth <= 1200) {
                    closeMenu();
                }
            });
        });
        
        window.addEventListener('resize', () => {
            if (window.innerWidth > 1200) {
                closeMenu();
            }
        });
        
        // Set progress bar widths dynamically
        document.addEventListener('DOMContentLoaded', function() {
            const progressBars = document.querySelectorAll('.progress-bar[data-width]');
            progressBars.forEach(bar => {
                const width = bar.getAttribute('data-width');
                bar.style.width = width + '%';
            });
        });
        
        console.log('Prescriptions page loaded successfully');
    </script>
</body>
</html>
