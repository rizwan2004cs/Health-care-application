<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Find Doctors - Bizil Health Care</title>
    <link rel="icon" type="image/png" href="/images/favicon.ico">
    <link rel="shortcut icon" type="image/png" href="/images/favicon.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/modern-dashboard.css">
    <style>
        body {
            background: #F8FAFC;
            min-height: 100vh;
            font-family: 'Inter', sans-serif;
            margin: 0;
            overflow-x: hidden;
            overflow-y: auto;
        }
        
        .search-header {
            background: linear-gradient(135deg, #8B5CF6 0%, #3B82F6 100%);
            color: white;
            padding: 2.5rem;
            border-radius: 16px;
            margin-bottom: 2rem;
            box-shadow: 0 10px 30px rgba(139, 92, 246, 0.2);
        }
        
        .search-header h1 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
        
        .search-header p {
            opacity: 0.9;
            font-size: 1rem;
            margin: 0;
        }
        
        .search-card {
            background: white;
            border-radius: 16px;
            padding: 2rem;
            border: 1px solid #E5E7EB;
            margin-bottom: 2rem;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
        }
        
        .search-form {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            align-items: end;
        }
        
        .form-group {
            display: flex;
            flex-direction: column;
        }
        
        .form-group label {
            font-weight: 600;
            color: #374151;
            margin-bottom: 0.5rem;
            font-size: 0.9375rem;
        }
        
        .form-group input,
        .form-group select {
            padding: 0.875rem 1rem;
            border: 1px solid #D1D5DB;
            border-radius: 10px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: white;
            color: #111827;
        }
        
        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: #8B5CF6;
            box-shadow: 0 0 0 3px rgba(139, 92, 246, 0.1);
        }
        
        .form-group input::placeholder {
            color: #9CA3AF;
        }
        
        .btn-search {
            background: #8B5CF6;
            color: white;
            padding: 0.875rem 2rem;
            border-radius: 10px;
            border: none;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }
        
        .btn-search:hover {
            background: #7C3AED;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
        }
        
        .results-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }
        
        .results-count {
            font-size: 1.125rem;
            color: #111827;
            font-weight: 600;
        }
        
        .doctors-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .doctor-card {
            background: white;
            border-radius: 16px;
            padding: 1.75rem;
            border: 1px solid #E5E7EB;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .doctor-card:hover {
            border-color: #8B5CF6;
            box-shadow: 0 8px 24px rgba(139, 92, 246, 0.15);
            transform: translateY(-4px);
        }
        
        .doctor-header {
            display: flex;
            gap: 1.25rem;
            margin-bottom: 1.5rem;
        }
        
        .doctor-avatar-large {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: linear-gradient(135deg, #8B5CF6, #3B82F6);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.75rem;
            font-weight: 700;
            flex-shrink: 0;
        }
        
        .doctor-info {
            flex: 1;
        }
        
        .doctor-name {
            font-size: 1.25rem;
            font-weight: 700;
            color: #111827;
            margin-bottom: 0.25rem;
        }
        
        .doctor-specialty {
            color: #8B5CF6;
            font-weight: 600;
            font-size: 0.9375rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 0.5rem;
        }
        
        .doctor-rating {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.875rem;
        }
        
        .stars {
            color: #F59E0B;
        }
        
        .rating-count {
            color: #6B7280;
        }
        
        .doctor-details {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 0.75rem;
            margin-bottom: 1.5rem;
        }
        
        .detail-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            font-size: 0.9375rem;
            color: #374151;
            min-width: 0;
        }
        
        .detail-item span {
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }
        
        .detail-item i {
            color: #8B5CF6;
            width: 20px;
            text-align: center;
        }
        
        .doctor-actions {
            display: flex;
            gap: 0.75rem;
        }
        
        .btn-action {
            padding: 0.75rem 1.5rem;
            border-radius: 10px;
            font-weight: 600;
            font-size: 0.9375rem;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            flex: 1;
        }
        
        .btn-action.primary {
            background: #8B5CF6;
            color: white;
        }
        
        .btn-action.primary:hover {
            background: #7C3AED;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(139, 92, 246, 0.3);
        }
        
        .btn-action.secondary {
            background: white;
            color: #6B7280;
            border: 1px solid #D1D5DB;
        }
        
        .btn-action.secondary:hover {
            background: #F9FAFB;
            border-color: #9CA3AF;
        }
        
        .verified-badge {
            background: #ECFDF5;
            color: #059669;
            padding: 0.25rem 0.75rem;
            border-radius: 50px;
            font-size: 0.75rem;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
        }
        
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: #6B7280;
        }
        
        .empty-state i {
            font-size: 4rem;
            color: #D1D5DB;
            margin-bottom: 1rem;
        }
        
        .empty-state h3 {
            font-size: 1.5rem;
            color: #374151;
            margin-bottom: 0.5rem;
        }
        
        .empty-state p {
            font-size: 1rem;
            margin-bottom: 1.5rem;
        }
        
        @media (max-width: 768px) {
            .search-form {
                grid-template-columns: 1fr;
            }
            
            .doctors-grid {
                grid-template-columns: 1fr;
            }
            
            .doctor-details {
                grid-template-columns: 1fr;
            }
            
            .doctor-actions {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar Overlay for Mobile -->
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    
    <!-- Modern Dashboard Layout -->
    <div class="dashboard">
        <!-- Sidebar Navigation -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="sidebar-logo">
                    <i class="fas fa-heartbeat"></i>
                </div>
                <div>
                    <h2 class="sidebar-title">Bizil Health</h2>
                </div>
            </div>
            
            <nav>
                <ul class="nav-menu">
                    <li class="nav-item">
                        <a href="/patient/dashboard" class="nav-link">
                            <i class="fas fa-home nav-icon"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/appointments" class="nav-link">
                            <i class="fas fa-calendar-alt nav-icon"></i>
                            <span>Appointments</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/search-doctors" class="nav-link active">
                            <i class="fas fa-user-md nav-icon"></i>
                            <span>Find Doctors</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/test-results" class="nav-link">
                            <i class="fas fa-flask nav-icon"></i>
                            <span>Test Results</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/prescriptions" class="nav-link">
                            <i class="fas fa-pills nav-icon"></i>
                            <span>Prescriptions</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/health-tips" class="nav-link">
                            <i class="fas fa-lightbulb nav-icon"></i>
                            <span>Health Tips</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="/patient/complete-profile" class="nav-link">
                            <i class="fas fa-user-edit nav-icon"></i>
                            <span>My Profile</span>
                        </a>
                    </li>
                    <li class="nav-item" style="margin-top: 2rem;">
                        <a href="/patient/logout" class="nav-link" style="color: #EF4444;">
                            <i class="fas fa-sign-out-alt nav-icon"></i>
                            <span>Logout</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>
        
        <!-- Main Content -->
        <main class="main-content">
            <!-- Top Header -->
            <header class="dashboard-header">
                <div class="welcome-section">
                    <div style="display: flex; align-items: center; gap: 1rem;">
                        <button class="mobile-menu-toggle" id="mobileMenuToggle" aria-label="Toggle Menu">
                            <i class="fas fa-bars"></i>
                        </button>
                        <div>
                            <h1 style="margin-bottom: 0.25rem;"><i class="fas fa-search"></i> Find Your Doctor</h1>
                            <p style="margin: 0;">Search and connect with verified healthcare professionals</p>
                        </div>
                    </div>
                </div>
                
                <div class="header-actions">
                    <div class="user-profile">
                        <div class="user-avatar">
                            <%= (patient && patient.firstName) ? patient.firstName.charAt(0).toUpperCase() : ((user && user.username) ? user.username.charAt(0).toUpperCase() : 'P') %>
                        </div>
                        <div class="user-info">
                            <div class="user-name"><%= (patient && patient.firstName) ? (patient.firstName + ' ' + (patient.lastName || '')) : ((user && user.username) ? user.username : 'Patient') %></div>
                            <div class="user-role">Patient</div>
                        </div>
                    </div>
                </div>
            </header>
            
            <!-- Success/Error Messages -->
            <% if (typeof successMessage !== 'undefined' && successMessage) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle me-2"></i><%= successMessage %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>
            
            <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="fas fa-exclamation-circle me-2"></i><%= errorMessage %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            <% } %>
            
            <!-- Search Card -->
            <div class="search-card">
                <form method="GET" action="/patient/search-doctors" class="search-form">
                    <div class="form-group">
                        <label for="specialty">Specialty</label>
                        <select id="specialty" name="specialty">
                            <option value="">All Specialties</option>
                            <option value="General Medicine" <%= (typeof specialty !== 'undefined' && specialty === 'General Medicine') ? 'selected' : '' %>>General Medicine</option>
                            <option value="Cardiology" <%= (typeof specialty !== 'undefined' && specialty === 'Cardiology') ? 'selected' : '' %>>Cardiology</option>
                            <option value="Neurology" <%= (typeof specialty !== 'undefined' && specialty === 'Neurology') ? 'selected' : '' %>>Neurology</option>
                            <option value="Orthopedics" <%= (typeof specialty !== 'undefined' && specialty === 'Orthopedics') ? 'selected' : '' %>>Orthopedics</option>
                            <option value="Pediatrics" <%= (typeof specialty !== 'undefined' && specialty === 'Pediatrics') ? 'selected' : '' %>>Pediatrics</option>
                            <option value="Gynecology" <%= (typeof specialty !== 'undefined' && specialty === 'Gynecology') ? 'selected' : '' %>>Gynecology</option>
                            <option value="Dermatology" <%= (typeof specialty !== 'undefined' && specialty === 'Dermatology') ? 'selected' : '' %>>Dermatology</option>
                            <option value="Psychiatry" <%= (typeof specialty !== 'undefined' && specialty === 'Psychiatry') ? 'selected' : '' %>>Psychiatry</option>
                            <option value="ENT" <%= (typeof specialty !== 'undefined' && specialty === 'ENT') ? 'selected' : '' %>>ENT</option>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <label for="searchName">Search by Name</label>
                        <input 
                            type="text" 
                            id="searchName" 
                            name="searchName" 
                            placeholder="Doctor name or specialty..."
                            value="<%= typeof searchName !== 'undefined' ? searchName : '' %>">
                    </div>
                    
                    <div class="form-group">
                        <label for="location">Location</label>
                        <input 
                            type="text" 
                            id="location" 
                            name="location" 
                            placeholder="City or area..."
                            value="<%= typeof location !== 'undefined' ? location : '' %>">
                    </div>
                    
                    <div class="form-group" style="align-self: end;">
                        <button type="submit" class="btn-search">
                            <i class="fas fa-search"></i>
                            Search
                        </button>
                    </div>
                </form>
            </div>
            
            <!-- Results Section -->
            <% if (typeof doctors !== 'undefined' && doctors && doctors.length > 0) { %>
                <div class="results-header">
                    <div class="results-count">
                        <i class="fas fa-users"></i>
                        Found <%= doctors.length %> <%= doctors.length === 1 ? 'doctor' : 'doctors' %>
                    </div>
                </div>
                
                <div class="doctors-grid">
                    <% doctors.forEach(function(doctor) { 
                        const initials = (doctor.firstName.charAt(0) + doctor.lastName.charAt(0)).toUpperCase();
                    %>
                        <div class="doctor-card">
                            <div class="doctor-header">
                                <div class="doctor-avatar-large"><%= initials %></div>
                                <div class="doctor-info">
                                    <h3 class="doctor-name">Dr. <%= doctor.firstName %> <%= doctor.lastName %></h3>
                                    <div class="doctor-specialty">
                                        <i class="fas fa-stethoscope"></i>
                                        <%= doctor.specialization || 'General Practice' %>
                                    </div>
                                    <% if (doctor.isVerified) { %>
                                        <span class="verified-badge">
                                            <i class="fas fa-check-circle"></i>
                                            Verified
                                        </span>
                                    <% } %>
                                </div>
                            </div>
                            
                            <div class="doctor-details">
                                <% if (doctor.currentHospital && doctor.currentHospital.name) { %>
                                <div class="detail-item">
                                    <i class="fas fa-hospital"></i>
                                    <span><%= doctor.currentHospital.name %></span>
                                </div>
                                <% } %>
                                
                                <% if (doctor.address && (doctor.address.city || doctor.address.state)) { %>
                                <div class="detail-item">
                                    <i class="fas fa-map-marker-alt"></i>
                                    <span>
                                        <% if (doctor.address.city) { %><%= doctor.address.city %><% } %>
                                        <% if (doctor.address.city && doctor.address.state) { %>, <% } %>
                                        <% if (doctor.address.state) { %><%= doctor.address.state %><% } %>
                                    </span>
                                </div>
                                <% } %>
                                
                                <% if (doctor.experience) { %>
                                <div class="detail-item">
                                    <i class="fas fa-user-clock"></i>
                                    <span><%= doctor.experience %> years exp.</span>
                                </div>
                                <% } %>
                                
                                <% if (doctor.consultationFee && doctor.consultationFee.inPerson) { %>
                                <div class="detail-item">
                                    <i class="fas fa-rupee-sign"></i>
                                    <span>₹<%= doctor.consultationFee.inPerson %></span>
                                </div>
                                <% } %>
                                
                                <% if (doctor.ratings && doctor.ratings.overall) { %>
                                <div class="detail-item">
                                    <div class="doctor-rating">
                                        <div class="stars">
                                            <% for(let i = 0; i < 5; i++) { %>
                                                <i class="fas fa-star<%= i < Math.round(doctor.ratings.overall) ? '' : '-o' %>"></i>
                                            <% } %>
                                        </div>
                                        <span class="rating-count">(<%= doctor.ratings.count || 0 %>)</span>
                                    </div>
                                </div>
                                <% } %>
                            </div>
                            
                            <div class="doctor-actions">
                                <a href="/patient/book-appointment/<%= doctor._id %>" class="btn-action primary">
                                    <i class="fas fa-calendar-plus"></i>
                                    Book Appointment
                                </a>
                                <a href="/patient/doctor-profile/<%= doctor._id %>" class="btn-action secondary">
                                    <i class="fas fa-user"></i>
                                    View Profile
                                </a>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="search-card">
                    <div class="empty-state">
                        <i class="fas fa-user-md"></i>
                        <h3>No doctors found</h3>
                        <p>Try adjusting your search criteria or browse all available doctors</p>
                        <a href="/patient/search-doctors" class="btn-action primary" style="display: inline-flex; width: auto;">
                            <i class="fas fa-refresh"></i>
                            Show All Doctors
                        </a>
                    </div>
                </div>
            <% } %>
        </main>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Mobile menu toggle
        const menuToggle = document.getElementById('mobileMenuToggle');
        const sidebar = document.querySelector('.sidebar');
        const overlay = document.getElementById('sidebarOverlay');
        
        if (menuToggle) {
            menuToggle.addEventListener('click', function() {
                sidebar.classList.add('active');
                if (overlay) overlay.classList.add('active');
            });
        }
        
        if (overlay) {
            overlay.addEventListener('click', function() {
                sidebar.classList.remove('active');
                overlay.classList.remove('active');
            });
        }
        
        // Close mobile menu on window resize
        window.addEventListener('resize', function() {
            if (window.innerWidth > 1200) {
                sidebar.classList.remove('active');
                if (overlay) overlay.classList.remove('active');
            }
        });
    </script>
</body>
</html>
